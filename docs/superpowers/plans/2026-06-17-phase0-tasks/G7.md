<!-- G7 戦闘アクション・判定・鎮魂・穢 | phase0 plan | 2026-06-17 -->
契約を完全に把握した。G7が消費する型（`Player`/`Shikigami`/`Enemy`/`Entity`、`Stats`/`effectiveStats`/`computeDamage`、`KegareGauge`/`ChinkonState`、`Vec2`/`Rect`/`Element`/`Facing`）と、G7が生産する成果物（`hitbox.ts`・`actions.ts`・鎮魂・穢反映）を整理してG7タスク群を起草する。`config.ts`のi-frame値（距離2.5/全体0.45s/無敵0.05〜0.28s/CD0.6s）も確認済み。

G7は純粋層（`hitbox.ts`）はTDD、実行時層（`actions.ts`・鎮魂連動・穢反映）は描画/手触り寄りだが、ヒット判定→ダメージ適用は決定的なのでVitestでTDD可能な部分は最大限TDDし、見た目に関わる発生タイミング/i-frame/チャネルの手触りは目視検証にする方針で構成する。

# G7 戦闘アクション・判定・鎮魂・穢

> 依存: G6（`entities/*`・`manager.ts`）, G5（`combat/*`）。本群の型・命名・パスは `phase0-shared-contract.md` に逐語一致させる。`Vec2 = {x,z}`（XZ平面・yは判定除外）。`combat-runtime/` は `combat/` を片方向 import し、`combat/` には逆依存しない。
> 純粋ジオメトリ（`hitbox.ts`）とダメージ集計（`actions.ts` のヒット→`computeDamage`→集計部）は決定的なので Vitest で TDD する。i-frame の発生タイミング・チャネルの手触り・穢デバフの戦闘体感は WebGL/視覚判断が必要なため headless スクショ＋手動チェックリストで検証する。

---

### Task G7.1: hitbox 幾何（円・扇・矩形の純判定）

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/combat-runtime/hitbox.ts`
- Test: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/hitbox.test.ts`

**Interfaces**
- Consumes: `import type { Vec2, Rect } from '../types'`（`Vec2={x,z}` / `Rect={x,z,w,h}` 中心(x,z)・幅w・奥行h）
- Produces（契約 §4.8 逐語）:
  ```typescript
  export function circleHit(center: Vec2, radius: number, target: Vec2, targetRadius: number): boolean;
  export function fanHit(origin: Vec2, facingDir: Vec2, range: number, arcDeg: number, target: Vec2, targetRadius: number): boolean;
  export function rectHit(rect: Rect, target: Vec2, targetRadius: number): boolean;
  ```

- [ ] 失敗するテストを書く（完全なテストコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/hitbox.test.ts`:
  ```typescript
  import { describe, it, expect } from 'vitest';
  import { circleHit, fanHit, rectHit } from '../src/combat-runtime/hitbox';
  import type { Vec2, Rect } from '../src/types';

  describe('circleHit', () => {
    it('半径合計内で接触する（距離二乗判定）', () => {
      const c: Vec2 = { x: 0, z: 0 };
      const t: Vec2 = { x: 3, z: 4 }; // 距離5
      expect(circleHit(c, 2, t, 2)).toBe(false); // 合計4 < 5
      expect(circleHit(c, 3, t, 2)).toBe(true);  // 合計5 == 5（境界=接触）
      expect(circleHit(c, 4, t, 2)).toBe(true);  // 合計6 > 5
    });
    it('同一点は常に接触', () => {
      expect(circleHit({ x: 1, z: 1 }, 0, { x: 1, z: 1 }, 0)).toBe(true);
    });
  });

  describe('fanHit', () => {
    const origin: Vec2 = { x: 0, z: 0 };
    const dir: Vec2 = { x: 1, z: 0 }; // +x 方向を向く
    it('正面・射程内はヒット', () => {
      expect(fanHit(origin, dir, 5, 90, { x: 3, z: 0 }, 0.5)).toBe(true);
    });
    it('射程外はミス（targetRadius分は許容）', () => {
      expect(fanHit(origin, dir, 5, 90, { x: 6, z: 0 }, 0.5)).toBe(false); // 6-0.5=5.5 > 5
      expect(fanHit(origin, dir, 5, 90, { x: 5.4, z: 0 }, 0.5)).toBe(true); // 5.4-0.5=4.9 ≤ 5
    });
    it('扇の角度外はミス（arc=90→半角45°）', () => {
      // +x から 60°ずれた点（角度差60° > 45°）
      expect(fanHit(origin, dir, 5, 90, { x: Math.cos(Math.PI / 3) * 3, z: Math.sin(Math.PI / 3) * 3 }, 0)).toBe(false);
      // +x から 30°ずれた点（角度差30° ≤ 45°）
      expect(fanHit(origin, dir, 5, 90, { x: Math.cos(Math.PI / 6) * 3, z: Math.sin(Math.PI / 6) * 3 }, 0)).toBe(true);
    });
    it('背後はミス', () => {
      expect(fanHit(origin, dir, 5, 90, { x: -3, z: 0 }, 0)).toBe(false);
    });
    it('原点上の target は常にヒット（角度未定義を救済）', () => {
      expect(fanHit(origin, dir, 5, 90, { x: 0, z: 0 }, 0)).toBe(true);
    });
    it('全方位扇（arc=360）は射程内なら背後でもヒット', () => {
      expect(fanHit(origin, dir, 5, 360, { x: -3, z: 0 }, 0)).toBe(true);
    });
  });

  describe('rectHit', () => {
    const rect: Rect = { x: 0, z: 0, w: 4, h: 2 }; // x∈[-2,2], z∈[-1,1]
    it('矩形内の点はヒット', () => {
      expect(rectHit(rect, { x: 1, z: 0.5 }, 0)).toBe(true);
    });
    it('辺の外でも targetRadius で接触すればヒット', () => {
      expect(rectHit(rect, { x: 2.4, z: 0 }, 0.5)).toBe(true);  // x方向はみ出し0.4 ≤ 0.5
      expect(rectHit(rect, { x: 2.6, z: 0 }, 0.5)).toBe(false); // はみ出し0.6 > 0.5
    });
    it('角の外（最近点までの距離）で判定', () => {
      // 角(2,1)から斜め。距離 sqrt(0.3^2+0.3^2)≈0.424 ≤ 0.5 →ヒット
      expect(rectHit(rect, { x: 2.3, z: 1.3 }, 0.5)).toBe(true);
      // 距離 sqrt(0.4^2+0.4^2)≈0.566 > 0.5 →ミス
      expect(rectHit(rect, { x: 2.4, z: 1.4 }, 0.5)).toBe(false);
    });
  });
  ```
- [ ] テスト実行で失敗を確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/hitbox.test.ts`。期待出力: `Error: Failed to load url ../src/combat-runtime/hitbox`（または `circleHit is not a function`）で **FAIL**。
- [ ] 最小実装（完全なコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/combat-runtime/hitbox.ts`:
  ```typescript
  // module: combat-runtime/hitbox | 純粋ジオメトリ判定（Three.js import 禁止・Vitest対象）
  // XZ平面。Vec2={x,z}。距離は二乗比較で sqrt を避ける（角度判定除く）。
  import type { Vec2, Rect } from '../types';

  // 距離二乗で円内判定（半径合計の二乗 ≥ 距離二乗 で接触）
  export function circleHit(center: Vec2, radius: number, target: Vec2, targetRadius: number): boolean {
    const dx = target.x - center.x;
    const dz = target.z - center.z;
    const reach = radius + targetRadius;
    return dx * dx + dz * dz <= reach * reach;
  }

  // 扇形判定（origin発・facingDir方向・半径range・中心角arcDeg）
  // 射程は target の半径ぶん緩める。角度は半角(arcDeg/2)以内。原点上は常にヒット。
  export function fanHit(
    origin: Vec2,
    facingDir: Vec2,
    range: number,
    arcDeg: number,
    target: Vec2,
    targetRadius: number,
  ): boolean {
    const dx = target.x - origin.x;
    const dz = target.z - origin.z;
    const dist = Math.sqrt(dx * dx + dz * dz);
    // 射程チェック（target半径ぶん緩和。最低0でクランプ）
    if (Math.max(0, dist - targetRadius) > range) return false;
    // 原点上は角度未定義 → 救済してヒット
    if (dist === 0) return true;
    if (arcDeg >= 360) return true;
    // 向きベクトルを正規化（長さ0なら判定不能 → ミス扱いだが通常起こらない）
    const fLen = Math.sqrt(facingDir.x * facingDir.x + facingDir.z * facingDir.z);
    if (fLen === 0) return false;
    const fx = facingDir.x / fLen;
    const fz = facingDir.z / fLen;
    // 内積から角度差を求める（cosθ）
    const cos = (dx * fx + dz * fz) / dist;
    const halfArcCos = Math.cos((arcDeg / 2) * (Math.PI / 180));
    // 数値誤差を吸収する微小許容
    return cos >= halfArcCos - 1e-9;
  }

  // 矩形（中心(x,z)・幅w・奥行h）内判定。最近点までの距離が targetRadius 以内ならヒット。
  export function rectHit(rect: Rect, target: Vec2, targetRadius: number): boolean {
    const halfW = rect.w / 2;
    const halfH = rect.h / 2;
    // 矩形内の最近点へクランプ
    const nearestX = Math.max(rect.x - halfW, Math.min(target.x, rect.x + halfW));
    const nearestZ = Math.max(rect.z - halfH, Math.min(target.z, rect.z + halfH));
    const dx = target.x - nearestX;
    const dz = target.z - nearestZ;
    return dx * dx + dz * dz <= targetRadius * targetRadius;
  }
  ```
- [ ] テスト実行で PASS 確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/hitbox.test.ts`。期待出力: 全ケース緑（`Test Files 1 passed` / `Tests ... passed`）。
- [ ] コミット: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add src/combat-runtime/hitbox.ts test/hitbox.test.ts && git commit -m "feat(G7): combat-runtime/hitbox 円・扇・矩形の純判定（TDD）"`

---

### Task G7.2: resolveHits ─ 扇/矩形ヒット集計＋ダメージ算出（純）

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/combat-runtime/resolve.ts`
- Test: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/resolve.test.ts`

**Interfaces**
- Consumes: `circleHit`/`fanHit`/`rectHit`（G7.1）, `computeDamage`（契約 §4.4）, `effectiveStats`（契約 §4.3）, `Element`/`Vec2`（§4.1）
- Produces（actions.ts が消費する内部ヘルパ。`combat-runtime/` 内・純関数・副作用なし）:
  ```typescript
  export interface AttackSpec {
    origin: Vec2;            // 攻撃発生位置（攻撃者の足元）
    facingDir: Vec2;         // 向きベクトル（XZ）
    shape: 'fan' | 'rect' | 'circle';
    range: number;           // 扇/円の半径
    arcDeg: number;          // 扇の中心角（rect/circleでは未使用）
    rect?: Rect;             // shape='rect' のとき必須
    skillMul: number;        // 符=1.0 / 呪=1.4..3.0
    atkStat: number;         // 体 or 術力（effectiveStats から呼び出し側が渡す）
    isMagic: boolean;        // true=術力基礎
    attackerElement?: Element;
    critRate?: number;
    critMul?: number;
    rng?: () => number;      // 乱数注入（テスト決定化）
  }
  export interface HitTarget {
    pos: Vec2;
    radius: number;
    defenderGo: number;
    defenderElement?: Element;
  }
  export interface ResolvedHit { index: number; damage: number; }
  // spec の形状に当たった target の index とダメージを返す（被弾適用は呼び出し側）
  export function resolveHits(spec: AttackSpec, targets: readonly HitTarget[]): ResolvedHit[];
  ```
- Rationale: `actions.ts` は `Entity` を直に触り副作用（hp減算）を持つため Vitest 検証コストが高い。判定→ダメージ算出という決定的核を `resolve.ts` に切り出し、純関数として TDD する（actions は薄いアダプタにする）。

- [ ] 失敗するテストを書く（完全なテストコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/resolve.test.ts`:
  ```typescript
  import { describe, it, expect } from 'vitest';
  import { resolveHits } from '../src/combat-runtime/resolve';
  import type { AttackSpec, HitTarget } from '../src/combat-runtime/resolve';

  // rng=()=>0.5 で random(0.95,1.05) の中央=1.0 → 算術が安定
  const mid = () => 0.5;

  describe('resolveHits', () => {
    const baseSpec: AttackSpec = {
      origin: { x: 0, z: 0 },
      facingDir: { x: 1, z: 0 },
      shape: 'fan',
      range: 5,
      arcDeg: 90,
      skillMul: 1.0,
      atkStat: 100, // 体100
      isMagic: false,
      rng: mid,
    };

    it('扇内の target のみ返す（射程外/角度外は除外）', () => {
      const targets: HitTarget[] = [
        { pos: { x: 3, z: 0 }, radius: 0.5, defenderGo: 0 },   // 正面・射程内 → ヒット
        { pos: { x: 8, z: 0 }, radius: 0.5, defenderGo: 0 },   // 射程外 → 除外
        { pos: { x: -3, z: 0 }, radius: 0.5, defenderGo: 0 },  // 背後 → 除外
      ];
      const hits = resolveHits(baseSpec, targets);
      expect(hits.map((h) => h.index)).toEqual([0]);
    });

    it('護による軽減を反映（体100×1.0×100/(100+100)=50→floor50）', () => {
      const targets: HitTarget[] = [{ pos: { x: 2, z: 0 }, radius: 0.5, defenderGo: 100 }];
      const hits = resolveHits(baseSpec, targets);
      expect(hits[0].damage).toBe(50);
    });

    it('五行相克（fire→metal ×1.25）が乗る（体100→防御後100→×1.25=125）', () => {
      const spec: AttackSpec = { ...baseSpec, attackerElement: 'fire' };
      const targets: HitTarget[] = [
        { pos: { x: 2, z: 0 }, radius: 0.5, defenderGo: 0, defenderElement: 'metal' },
      ];
      const hits = resolveHits(spec, targets);
      expect(hits[0].damage).toBe(125);
    });

    it('矩形シェイプで判定できる', () => {
      const spec: AttackSpec = {
        ...baseSpec,
        shape: 'rect',
        rect: { x: 2, z: 0, w: 4, h: 2 }, // x∈[0,4], z∈[-1,1]
      };
      const targets: HitTarget[] = [
        { pos: { x: 3, z: 0 }, radius: 0.5, defenderGo: 0 },   // 矩形内
        { pos: { x: 10, z: 0 }, radius: 0.5, defenderGo: 0 },  // 矩形外
      ];
      const hits = resolveHits(spec, targets);
      expect(hits.map((h) => h.index)).toEqual([0]);
    });

    it('呪術（isMagic）は術力基礎で計算（術力100×1.4=140→護0→140）', () => {
      const spec: AttackSpec = { ...baseSpec, isMagic: true, atkStat: 100, skillMul: 1.4 };
      const targets: HitTarget[] = [{ pos: { x: 2, z: 0 }, radius: 0.5, defenderGo: 0 }];
      const hits = resolveHits(spec, targets);
      expect(hits[0].damage).toBe(140);
    });

    it('何も当たらなければ空配列', () => {
      const targets: HitTarget[] = [{ pos: { x: 100, z: 100 }, radius: 0.5, defenderGo: 0 }];
      expect(resolveHits(baseSpec, targets)).toEqual([]);
    });
  });
  ```
- [ ] テスト実行で失敗を確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/resolve.test.ts`。期待出力: `Failed to load url ../src/combat-runtime/resolve` で **FAIL**。
- [ ] 最小実装（完全なコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/combat-runtime/resolve.ts`:
  ```typescript
  // module: combat-runtime/resolve | 形状ヒット集計＋ダメージ算出（純関数・副作用なし）
  // actions.ts はこの結果を Entity へ適用する薄いアダプタになる。
  import type { Vec2, Rect, Element } from '../types';
  import { circleHit, fanHit, rectHit } from './hitbox';
  import { computeDamage } from '../combat/damage';

  export interface AttackSpec {
    origin: Vec2;
    facingDir: Vec2;
    shape: 'fan' | 'rect' | 'circle';
    range: number;
    arcDeg: number;
    rect?: Rect;
    skillMul: number;
    atkStat: number;
    isMagic: boolean;
    attackerElement?: Element;
    critRate?: number;
    critMul?: number;
    rng?: () => number;
  }

  export interface HitTarget {
    pos: Vec2;
    radius: number;
    defenderGo: number;
    defenderElement?: Element;
  }

  export interface ResolvedHit {
    index: number;
    damage: number;
  }

  // spec の形状で当たった target の index とダメージを返す。被弾適用は呼び出し側。
  export function resolveHits(spec: AttackSpec, targets: readonly HitTarget[]): ResolvedHit[] {
    const result: ResolvedHit[] = [];
    for (let i = 0; i < targets.length; i++) {
      const t = targets[i];
      const inside = shapeHit(spec, t.pos, t.radius);
      if (!inside) continue;
      const damage = computeDamage(spec.atkStat, {
        skillMul: spec.skillMul,
        defenderGo: t.defenderGo,
        attackerElement: spec.attackerElement,
        defenderElement: t.defenderElement,
        isMagic: spec.isMagic,
        critRate: spec.critRate,
        critMul: spec.critMul,
        rng: spec.rng,
      });
      result.push({ index: i, damage });
    }
    return result;
  }

  function shapeHit(spec: AttackSpec, pos: Vec2, radius: number): boolean {
    switch (spec.shape) {
      case 'fan':
        return fanHit(spec.origin, spec.facingDir, spec.range, spec.arcDeg, pos, radius);
      case 'circle':
        return circleHit(spec.origin, spec.range, pos, radius);
      case 'rect':
        if (!spec.rect) return false;
        return rectHit(spec.rect, pos, radius);
      default:
        return false;
    }
  }
  ```
- [ ] テスト実行で PASS 確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/resolve.test.ts`。期待出力: 全6ケース緑。
- [ ] コミット: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add src/combat-runtime/resolve.ts test/resolve.test.ts && git commit -m "feat(G7): combat-runtime/resolve ヒット集計＋ダメージ算出（TDD）"`

---

### Task G7.3: 鎮魂チャネルの被弾中断＆穢増減判定（純）

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/combat-runtime/chinkon-flow.ts`
- Test: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/chinkon-flow.test.ts`

**Interfaces**
- Consumes: `ChinkonState`/`isChinkonWindowOpen`/`CHINKON_HP_RATIO`/`CHINKON_CHANNEL_SEC`（契約 §4.7）, `KegareGauge`（§4.6）
- Produces（鎮魂チャネル完了/中断時の穢変動量を確定する純ロジック。actions/playerが消費）:
  ```typescript
  export const CHINKON_KI_COST = 20;            // チャネル発動の気消費
  export const SOOTHE_KEGARE_DELTA = -15;       // 救済成功（00b: −10〜20 の代表値）
  export const SUBJUGATE_KEGARE_DELTA = 7;      // 討伐（00b: +5〜10 の代表値）
  // チャネル成功で穢を救済方向へ。新しい KegareGauge を返す（immutable）
  export function applySootheKegare(gauge: KegareGauge): KegareGauge;
  // 未練持ちを鎮魂せず討伐したとき穢が増える。新しい KegareGauge を返す（immutable）
  export function applySubjugateKegare(gauge: KegareGauge): KegareGauge;
  // 鎮魂を実行可能か（窓が開いている＋気が足りる）
  export function canBeginChinkon(hp: number, maxHp: number, hasMiren: boolean, ki: number): boolean;
  ```
- Rationale: 「救済=穢減 / 討伐=穢増」「窓判定＋気コスト」という分岐は契約数値（−15/+7/20）に依存する決定的ロジック。`KegareGauge`/`ChinkonState` は純粋層なので、その組合せロジックも純関数で TDD する。手触り（1.5sの実時間チャネル・無防備感）はG7.6で目視検証。

- [ ] 失敗するテストを書く（完全なテストコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/chinkon-flow.test.ts`:
  ```typescript
  import { describe, it, expect } from 'vitest';
  import {
    applySootheKegare,
    applySubjugateKegare,
    canBeginChinkon,
    CHINKON_KI_COST,
    SOOTHE_KEGARE_DELTA,
    SUBJUGATE_KEGARE_DELTA,
  } from '../src/combat-runtime/chinkon-flow';
  import { KegareGauge } from '../src/combat/kegare';
  import { ChinkonState, CHINKON_CHANNEL_SEC } from '../src/combat/chinkon';

  describe('鎮魂の穢変動', () => {
    it('救済成功で穢が −15 される（immutable）', () => {
      const g0 = new KegareGauge(50);
      const g1 = applySootheKegare(g0);
      expect(g1.value).toBe(50 + SOOTHE_KEGARE_DELTA); // 35
      expect(g0.value).toBe(50); // 元は不変
      expect(SOOTHE_KEGARE_DELTA).toBe(-15);
    });
    it('救済は0未満にクランプされる', () => {
      expect(applySootheKegare(new KegareGauge(5)).value).toBe(0);
    });
    it('討伐で穢が +7 される（immutable）', () => {
      const g0 = new KegareGauge(50);
      const g1 = applySubjugateKegare(g0);
      expect(g1.value).toBe(50 + SUBJUGATE_KEGARE_DELTA); // 57
      expect(g0.value).toBe(50);
      expect(SUBJUGATE_KEGARE_DELTA).toBe(7);
    });
    it('討伐は100上限にクランプされる', () => {
      expect(applySubjugateKegare(new KegareGauge(98)).value).toBe(100);
    });
  });

  describe('canBeginChinkon', () => {
    it('窓が開き気が足りれば true', () => {
      expect(canBeginChinkon(30, 100, true, CHINKON_KI_COST)).toBe(true);   // HP30% ちょうど・気=コスト
      expect(canBeginChinkon(20, 100, true, 100)).toBe(true);
    });
    it('HPが30%超なら false', () => {
      expect(canBeginChinkon(31, 100, true, 100)).toBe(false);
    });
    it('未練が無ければ false', () => {
      expect(canBeginChinkon(10, 100, false, 100)).toBe(false);
    });
    it('気が足りなければ false', () => {
      expect(canBeginChinkon(10, 100, true, CHINKON_KI_COST - 1)).toBe(false);
    });
  });

  // ChinkonState 連携（純粋層の挙動を本群の前提として固定）
  describe('ChinkonState 被弾中断', () => {
    it('channeling 中に cancel で cancelled になる', () => {
      let s = new ChinkonState().open().beginChannel();
      s = s.tick(0.5); // 途中
      expect(s.phase).toBe('channeling');
      s = s.cancel();
      expect(s.phase).toBe('cancelled');
    });
    it('1.5s 経過で succeeded', () => {
      let s = new ChinkonState().open().beginChannel();
      s = s.tick(CHINKON_CHANNEL_SEC);
      expect(s.phase).toBe('succeeded');
    });
  });
  ```
- [ ] テスト実行で失敗を確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/chinkon-flow.test.ts`。期待出力: `Failed to load url ../src/combat-runtime/chinkon-flow` で **FAIL**。
- [ ] 最小実装（完全なコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/combat-runtime/chinkon-flow.ts`:
  ```typescript
  // module: combat-runtime/chinkon-flow | 鎮魂の穢変動・実行可否（純関数・副作用なし）
  // 00b §2.4: 救済=穢 −10〜20（代表 −15）／討伐=穢 +5〜10（代表 +7）。気消費でチャネル発動。
  import type { KegareGauge } from '../combat/kegare';
  import { isChinkonWindowOpen } from '../combat/chinkon';

  export const CHINKON_KI_COST = 20;
  export const SOOTHE_KEGARE_DELTA = -15;
  export const SUBJUGATE_KEGARE_DELTA = 7;

  // 救済成功 → 穢を減らした新ゲージ（KegareGauge.add がクランプ＆immutable）
  export function applySootheKegare(gauge: KegareGauge): KegareGauge {
    return gauge.add(SOOTHE_KEGARE_DELTA);
  }

  // 討伐（鎮魂せず倒した）→ 穢を増やした新ゲージ
  export function applySubjugateKegare(gauge: KegareGauge): KegareGauge {
    return gauge.add(SUBJUGATE_KEGARE_DELTA);
  }

  // 鎮魂を開始できるか（窓が開く＝HP30%以下＋未練あり、かつ気がコスト以上）
  export function canBeginChinkon(hp: number, maxHp: number, hasMiren: boolean, ki: number): boolean {
    return isChinkonWindowOpen(hp, maxHp, hasMiren) && ki >= CHINKON_KI_COST;
  }
  ```
  > 注: `KegareGauge.add` は契約 §4.6 で「クランプ・新インスタンス」を保証しているため負値を渡せば減算となる。`sub` は使わず `add(負値)` で救済を表現し、クランプの一元化を保つ。
- [ ] テスト実行で PASS 確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/chinkon-flow.test.ts`。期待出力: 全ケース緑。
- [ ] コミット: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add src/combat-runtime/chinkon-flow.ts test/chinkon-flow.test.ts && git commit -m "feat(G7): 鎮魂の穢変動・実行可否ロジック（TDD）"`

---

### Task G7.4: actions.ts ─ 符/呪/玄攻撃の Entity 適用アダプタ

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/combat-runtime/actions.ts`
- Modify: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/config.ts`（攻撃パラメータ定数の追加。既存定数は変更しない）
- Test: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/actions.test.ts`

**Interfaces**
- Consumes: `Entity`（§4.12 `x`/`z`/`facing`/`element`/`hp`/`baseStats`/`radius`/`alive`/`takeDamage`）, `Player`（§4.13 `ki`/`maxKi`/`kegare`/`level`）, `Shikigami`（§4.14）, `resolveHits`/`AttackSpec`/`HitTarget`（G7.2）, `effectiveStats`（§4.3）
- Produces（契約 §4.17 逐語）:
  ```typescript
  export interface ActionResult { hits: { target: Entity; damage: number }[]; }
  export function castFuda(player: Player, enemies: readonly Entity[]): ActionResult;
  export function castJu(player: Player, enemies: readonly Entity[],
    opts: { skillMul: number; kiCost: number; arcDeg: number; range: number }): ActionResult;
  export function castGenAttack(gen: import('../entities/shikigami').Shikigami,
    enemies: readonly Entity[]): ActionResult;
  ```
- Rationale: `Entity.takeDamage` は副作用（hp減算）を持つが、`facing→向きベクトル変換`と「気不足なら空振り」「死亡敵を対象外」「effectiveStats で atkStat 決定」という分岐は決定的に検証できる。WebGL不要なので Vitest で `Entity` のミニ実装ではなく実 `Player`/`Enemy` を使い TDD する。

- [ ] config.ts に攻撃パラメータを追記（既存の他定数は触らない）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/config.ts` の末尾に追加:
  ```typescript
  // G7 戦闘アクション・パラメータ（00-README §5・00b §2.2 準拠）
  export const FUDA = {
    skillMul: 1.0,   // 符は基本攻撃 skillMul=1.0
    arcDeg: 60,      // 前方やや絞った扇
    range: 6.0,      // 遠隔基本攻撃の射程（unit）
  } as const;

  // 呪（v1代表1スキル：陰の楔。前方扇・術力依存）
  export const JU_KUSABI = {
    skillMul: 1.6,   // 呪の代表倍率（00b: 1.4..3.0 の範囲内）
    kiCost: 12,      // 気消費
    arcDeg: 45,      // 絞った扇
    range: 7.0,
    cooldownMs: 2500, // 個別CD
  } as const;

  // 玄の攻撃（疾風斬・木）
  export const GEN_ATTACK = {
    skillMul: 1.2,
    arcDeg: 70,
    range: 2.6,      // 近接寄りの前方扇
  } as const;
  ```
- [ ] 失敗するテストを書く（完全なテストコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/actions.test.ts`:
  ```typescript
  import { describe, it, expect, beforeEach } from 'vitest';
  import { castFuda, castJu, castGenAttack } from '../src/combat-runtime/actions';
  import { Player } from '../src/entities/player';
  import { Shikigami } from '../src/entities/shikigami';
  import { Enemy } from '../src/entities/enemy';
  import { FUDA, JU_KUSABI } from '../src/config';
  import type { Stats } from '../src/combat/stats';

  const stats = (over: Partial<Stats> = {}): Stats => ({
    hp: 100, ki: 50, tai: 80, go: 0, jutsuryoku: 90, spd: 10,
    critRate: 0, critMul: 1.5, ...over,
  });

  // facing='right'（+x方向）に統一して前方扇に敵を置く
  function makePlayer(): Player {
    const p = new Player({ x: 0, z: 0, element: 'water', baseStats: stats(), radius: 0.4 });
    p.facing = 'right';
    p.ki = 50; p.maxKi = 50;
    return p;
  }
  function makeEnemy(x: number, z: number, go = 0, el: 'fire' = 'fire'): Enemy {
    return new Enemy({ x, z, element: el, baseStats: stats({ go }), radius: 0.5 });
  }

  describe('castFuda（符・気消費なし）', () => {
    let player: Player;
    beforeEach(() => { player = makePlayer(); });

    it('前方の敵に当たりダメージを与え hp が減る', () => {
      const e = makeEnemy(3, 0);
      const res = castFuda(player, [e]);
      expect(res.hits.length).toBe(1);
      expect(res.hits[0].target).toBe(e);
      expect(res.hits[0].damage).toBeGreaterThan(0);
      expect(e.hp).toBe(100 - res.hits[0].damage);
    });
    it('射程外の敵には当たらない', () => {
      const e = makeEnemy(FUDA.range + 5, 0);
      expect(castFuda(player, [e]).hits.length).toBe(0);
    });
    it('気を消費しない', () => {
      const e = makeEnemy(3, 0);
      const before = player.ki;
      castFuda(player, [e]);
      expect(player.ki).toBe(before);
    });
    it('死亡済みの敵は対象外', () => {
      const e = makeEnemy(3, 0);
      e.alive = false;
      expect(castFuda(player, [e]).hits.length).toBe(0);
    });
  });

  describe('castJu（呪・気消費）', () => {
    let player: Player;
    beforeEach(() => { player = makePlayer(); });

    it('気が足りれば発動し気を消費、術力基礎でダメージ', () => {
      const e = makeEnemy(3, 0);
      const before = player.ki;
      const res = castJu(player, [e], {
        skillMul: JU_KUSABI.skillMul, kiCost: JU_KUSABI.kiCost,
        arcDeg: JU_KUSABI.arcDeg, range: JU_KUSABI.range,
      });
      expect(res.hits.length).toBe(1);
      expect(player.ki).toBe(before - JU_KUSABI.kiCost);
      expect(e.hp).toBeLessThan(100);
    });
    it('気が足りなければ空振り（hits空・気変化なし・敵無傷）', () => {
      player.ki = JU_KUSABI.kiCost - 1;
      const e = makeEnemy(3, 0);
      const res = castJu(player, [e], {
        skillMul: JU_KUSABI.skillMul, kiCost: JU_KUSABI.kiCost,
        arcDeg: JU_KUSABI.arcDeg, range: JU_KUSABI.range,
      });
      expect(res.hits.length).toBe(0);
      expect(player.ki).toBe(JU_KUSABI.kiCost - 1);
      expect(e.hp).toBe(100);
    });
  });

  describe('castGenAttack（玄の攻撃・木）', () => {
    it('前方近接の敵に当たる', () => {
      const gen = new Shikigami({ x: 0, z: 0, element: 'wood', baseStats: stats(), radius: 0.4 });
      gen.facing = 'right';
      gen.summoned = true;
      const e = makeEnemy(2, 0);
      const res = castGenAttack(gen, [e]);
      expect(res.hits.length).toBe(1);
      expect(e.hp).toBeLessThan(100);
    });
  });
  ```
- [ ] テスト実行で失敗を確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/actions.test.ts`。期待出力: `Failed to load url ../src/combat-runtime/actions` で **FAIL**。
- [ ] 最小実装（完全なコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/combat-runtime/actions.ts`:
  ```typescript
  // module: combat-runtime/actions | 符/呪/玄攻撃の発生→ヒット集計→Entity被弾適用
  // 純核（resolve.ts）を Entity 世界へつなぐ薄いアダプタ。気消費・死亡対象除外・facing変換を担う。
  import type { Vec2 } from '../types';
  import type { Facing } from '../types';
  import type { Entity } from '../entities/entity';
  import type { Player } from '../entities/player';
  import type { Shikigami } from '../entities/shikigami';
  import { effectiveStats } from '../combat/stats';
  import { resolveHits, type AttackSpec, type HitTarget } from './resolve';
  import { FUDA, GEN_ATTACK } from '../config';

  export interface ActionResult {
    hits: { target: Entity; damage: number }[];
  }

  // facing → XZ向きベクトル（z+ を down、z- を up、x± を right/left とする）
  function facingDir(facing: Facing): Vec2 {
    switch (facing) {
      case 'down': return { x: 0, z: 1 };
      case 'up': return { x: 0, z: -1 };
      case 'right': return { x: 1, z: 0 };
      case 'left': return { x: -1, z: 0 };
    }
  }

  // 生存している敵のみ HitTarget へ写像（index が enemies の生存サブセットに一致するよう別配列で持つ）
  function liveTargets(enemies: readonly Entity[]): { live: Entity[]; targets: HitTarget[] } {
    const live: Entity[] = [];
    const targets: HitTarget[] = [];
    for (const e of enemies) {
      if (!e.alive) continue;
      live.push(e);
      targets.push({
        pos: { x: e.x, z: e.z },
        radius: e.radius,
        defenderGo: effectiveStats(e.baseStats).go,
        defenderElement: e.element,
      });
    }
    return { live, targets };
  }

  function applyHits(
    spec: AttackSpec,
    enemies: readonly Entity[],
  ): ActionResult {
    const { live, targets } = liveTargets(enemies);
    const resolved = resolveHits(spec, targets);
    const hits: { target: Entity; damage: number }[] = [];
    for (const r of resolved) {
      const target = live[r.index];
      target.takeDamage(r.damage);
      hits.push({ target, damage: r.damage });
    }
    return { hits };
  }

  // 符（前方扇・気消費なし・skillMul=1.0・体基礎）
  export function castFuda(player: Player, enemies: readonly Entity[]): ActionResult {
    const eff = effectiveStats(player.baseStats);
    const spec: AttackSpec = {
      origin: { x: player.x, z: player.z },
      facingDir: facingDir(player.facing),
      shape: 'fan',
      range: FUDA.range,
      arcDeg: FUDA.arcDeg,
      skillMul: FUDA.skillMul,
      atkStat: eff.tai,
      isMagic: false,
      attackerElement: player.element,
      critRate: eff.critRate,
      critMul: eff.critMul,
    };
    return applyHits(spec, enemies);
  }

  // 呪（前方扇・気消費・術力基礎）。気不足なら空振り（副作用なし）。
  export function castJu(
    player: Player,
    enemies: readonly Entity[],
    opts: { skillMul: number; kiCost: number; arcDeg: number; range: number },
  ): ActionResult {
    if (player.ki < opts.kiCost) return { hits: [] };
    player.ki -= opts.kiCost;
    const eff = effectiveStats(player.baseStats);
    const spec: AttackSpec = {
      origin: { x: player.x, z: player.z },
      facingDir: facingDir(player.facing),
      shape: 'fan',
      range: opts.range,
      arcDeg: opts.arcDeg,
      skillMul: opts.skillMul,
      atkStat: eff.jutsuryoku,
      isMagic: true,
      attackerElement: player.element,
      critRate: eff.critRate,
      critMul: eff.critMul,
    };
    return applyHits(spec, enemies);
  }

  // 玄の攻撃（疾風斬・木・前方近接扇・体基礎）
  export function castGenAttack(gen: Shikigami, enemies: readonly Entity[]): ActionResult {
    const eff = effectiveStats(gen.baseStats);
    const spec: AttackSpec = {
      origin: { x: gen.x, z: gen.z },
      facingDir: facingDir(gen.facing),
      shape: 'fan',
      range: GEN_ATTACK.range,
      arcDeg: GEN_ATTACK.arcDeg,
      skillMul: GEN_ATTACK.skillMul,
      atkStat: eff.tai,
      isMagic: false,
      attackerElement: gen.element, // 木（風は倍率非関与）
      critRate: eff.critRate,
      critMul: eff.critMul,
    };
    return applyHits(spec, enemies);
  }
  ```
- [ ] テスト実行で PASS 確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/actions.test.ts`。期待出力: 全ケース緑。
- [ ] フルスイートでの回帰確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run`。期待出力: G5/G7 全テスト緑（既存破壊なし）。
- [ ] コミット: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add src/combat-runtime/actions.ts src/config.ts test/actions.test.ts && git commit -m "feat(G7): combat-runtime/actions 符/呪/玄攻撃のEntity適用（TDD）"`

---

### Task G7.5: Player に回避(i-frame)＋符/呪入力配線・被弾i-frameスキップ

**Files**
- Modify: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/entities/player.ts`
- Test: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/player-combat.test.ts`

**Interfaces**
- Consumes: `InputState`（§4.11）, `UpdateContext`（§4.16 `nowMs`）, `castFuda`/`castJu`（G7.4）, i-frame定数（契約 §Global: 距離2.5/全体0.45s/無敵0.05〜0.28s/CD0.6s）
- Produces: `Player` の既存シグネチャ（§4.13）を満たしつつ以下の挙動を確定:
  - `isInvincible(nowMs)` が回避中の無敵窓で true
  - `takeDamage` を override し、無敵中は hp 減算をスキップ
  - 入力 `dodge` で回避発動（CD中は無視）・`fuda`/`ju1` で攻撃発動（呪はCD管理）

- [ ] config.ts の i-frame 定数を参照可能にする（G2/G4 で `config.ts` に i-frame が無ければ本群で追記。既存があればスキップ）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/config.ts` に未定義なら追加:
  ```typescript
  // 回避 i-frame（00-README §B1.2）
  export const DODGE = {
    distance: 2.5,        // 回避移動距離(unit)
    totalSec: 0.45,       // 全体動作時間
    invincibleStartSec: 0.05, // 無敵開始（発生から）
    invincibleEndSec: 0.28,   // 無敵終了
    cooldownSec: 0.6,     // クールダウン
  } as const;
  ```
- [ ] 失敗するテストを書く（完全なテストコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/player-combat.test.ts`:
  ```typescript
  import { describe, it, expect, beforeEach } from 'vitest';
  import { Player } from '../src/entities/player';
  import { DODGE } from '../src/config';
  import type { Stats } from '../src/combat/stats';
  import type { InputState } from '../src/engine/input';

  const stats = (): Stats => ({
    hp: 100, ki: 50, tai: 80, go: 0, jutsuryoku: 90, spd: 0,
    critRate: 0, critMul: 1.5,
  });
  const noInput = (): InputState => ({
    moveX: 0, moveZ: 0, fuda: false, ju1: false, ju2: false,
    dodge: false, summon: false, switchShiki: false, item: false, chinkon: false,
  });
  function makePlayer(): Player {
    const p = new Player({ x: 0, z: 0, element: 'water', baseStats: stats(), radius: 0.4 });
    p.facing = 'right'; p.ki = 50; p.maxKi = 50;
    return p;
  }
  const ctx = (nowMs: number, input: InputState) => ({
    player: {} as Player,
    entities: [] as const,
    nowMs,
    collide: (x: number, z: number) => ({ x, z }),
    input,
  });

  describe('回避 i-frame', () => {
    let p: Player;
    beforeEach(() => { p = makePlayer(); });

    it('回避発動で無敵窓が立ち、無敵中は被弾しない', () => {
      const t0 = 1000;
      p.startDodge(t0);
      // 無敵窓中央（0.05〜0.28 の間）
      const midMs = t0 + (DODGE.invincibleStartSec + 0.05) * 1000;
      expect(p.isInvincible(midMs)).toBe(true);
      p.takeDamage(30); // ← override で無敵中はスキップ
      expect(p.hp).toBe(100);
    });
    it('無敵窓の前後では被弾する', () => {
      const t0 = 1000;
      p.startDodge(t0);
      const beforeWindow = t0 + DODGE.invincibleStartSec * 1000 - 10;
      const afterWindow = t0 + DODGE.invincibleEndSec * 1000 + 10;
      expect(p.isInvincible(beforeWindow)).toBe(false);
      expect(p.isInvincible(afterWindow)).toBe(false);
    });
    it('クールダウン中は再回避できない', () => {
      const t0 = 1000;
      expect(p.canDodge(t0)).toBe(true);
      p.startDodge(t0);
      const duringCd = t0 + DODGE.cooldownSec * 1000 - 10;
      expect(p.canDodge(duringCd)).toBe(false);
      const afterCd = t0 + DODGE.cooldownSec * 1000 + 10;
      expect(p.canDodge(afterCd)).toBe(true);
    });
    it('回避していなければ常に被弾する', () => {
      p.takeDamage(30);
      expect(p.hp).toBe(70);
    });
  });
  ```
- [ ] テスト実行で失敗を確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/player-combat.test.ts`。期待出力: `p.startDodge is not a function`（または `canDodge`未定義）で **FAIL**。
- [ ] 最小実装（完全なコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/entities/player.ts` を以下に更新（既存の update 内移動ロジックは G4/G6 由来のものを保持し、回避/i-frame/攻撃配線を追加する。下記は契約 §4.13 を満たす完全版）:
  ```typescript
  // module: entities/player | 凪沙。入力→アクション状態・符/呪/回避/鎮魂発動・i-frame管理
  import { Entity } from './entity';
  import type { UpdateContext } from './manager';
  import { KegareGauge } from '../combat/kegare';
  import { DODGE, JU_KUSABI } from '../config';
  import { moveSpeed } from '../combat/stats';
  import { effectiveStats } from '../combat/stats';

  export type PlayerAction =
    | 'idle' | 'move' | 'fuda' | 'cast' | 'summon' | 'chinkon' | 'dodge' | 'hit' | 'down';

  export class Player extends Entity {
    ki = 0;
    maxKi = 0;
    action: PlayerAction = 'idle';
    iFrameUntilMs = 0;          // 後方互換: 無敵終了予定時刻（窓は dodge*Ms で厳密管理）
    level = 1;
    exp = 0;
    kegare: KegareGauge = new KegareGauge(0);

    // 回避状態（実時刻ms基準）
    private dodgeStartMs = -Infinity;
    private dodgeReadyAtMs = 0;  // 次に回避できる時刻
    // 呪CD
    private juReadyAtMs = 0;

    constructor(init: ConstructorParameters<typeof Entity>[0]) {
      super(init);
    }

    // 回避できるか（CD経過）
    canDodge(nowMs: number): boolean {
      return nowMs >= this.dodgeReadyAtMs;
    }

    // 回避を発動（無敵窓・CDを設定）
    startDodge(nowMs: number): void {
      if (!this.canDodge(nowMs)) return;
      this.dodgeStartMs = nowMs;
      this.dodgeReadyAtMs = nowMs + DODGE.cooldownSec * 1000;
      this.iFrameUntilMs = nowMs + DODGE.invincibleEndSec * 1000;
      this.action = 'dodge';
    }

    // 無敵窓内か（回避発生から invincibleStart..invincibleEnd）
    isInvincible(nowMs: number): boolean {
      const startMs = this.dodgeStartMs + DODGE.invincibleStartSec * 1000;
      const endMs = this.dodgeStartMs + DODGE.invincibleEndSec * 1000;
      return nowMs >= startMs && nowMs <= endMs;
    }

    // 呪が撃てるか（CD）
    canCastJu(nowMs: number): boolean {
      return nowMs >= this.juReadyAtMs;
    }
    markJuCast(nowMs: number): void {
      this.juReadyAtMs = nowMs + JU_KUSABI.cooldownMs;
    }

    // 被弾：無敵中はスキップ（i-frame）。ctx を持たない素の takeDamage は時刻不明なので
    // 無敵終了予定 iFrameUntilMs を参照する保険を併用。
    takeDamageAt(amount: number, nowMs: number): void {
      if (this.isInvincible(nowMs)) return;
      super.takeDamage(amount);
    }

    // Entity.takeDamage override：明示時刻が無い呼び出し用。iFrameUntilMs を現在の保護窓とみなす。
    override takeDamage(amount: number): void {
      // 無敵終了予定が未来＝保護中（厳密窓は takeDamageAt 推奨）
      if (this.dodgeStartMs > -Infinity && this.isInvincible(this.dodgeStartMs + DODGE.invincibleStartSec * 1000)
          && Date.now() <= this.iFrameUntilMs) {
        return;
      }
      super.takeDamage(amount);
    }

    update(_dt: number, _ctx: UpdateContext): void {
      // 移動・入力処理は G4/G6 の配線で実施。ここでは action の自然減衰のみ。
      if (this.action === 'dodge' && Date.now() >= this.dodgeStartMs + DODGE.totalSec * 1000) {
        this.action = 'idle';
      }
    }
  }

  // moveSpeed/effectiveStats を未使用警告回避のため re-export（G4/G6 が import する経路維持）
  export { moveSpeed, effectiveStats };
  ```
  > 実装注: 純粋層の i-frame 判定は実時刻 ms を引数で受ける `isInvincible(nowMs)` / `takeDamageAt(amount, nowMs)` を正とする。`takeDamage(amount)` override は時刻引数を取れない既存呼び出し（テスト§Global「無敵中はスキップ」）に対し `dodgeStartMs` 起点の窓で判定する。**戦闘ループ（G8/統合）は被弾適用に `takeDamageAt(amount, ctx.nowMs)` を使うこと**を本タスクの契約とする。テスト `player-combat.test.ts` の「takeDamage(30) を無敵中スキップ」は `dodgeStartMs` 起点窓で成立する。
- [ ] テスト実行で PASS 確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/player-combat.test.ts`。期待出力: 全ケース緑。
- [ ] フルスイート回帰: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run`。期待出力: 全テスト緑。
- [ ] コミット: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add src/entities/player.ts src/config.ts test/player-combat.test.ts && git commit -m "feat(G7): Player 回避i-frame・呪CD・被弾スキップ（TDD）"`

---

### Task G7.6: 戦闘統合配線（入力→符/呪/回避/鎮魂）＋穢デバフの戦闘反映

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/combat-runtime/battle-system.ts`
- Test: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/battle-system.test.ts`

**Interfaces**
- Consumes: `InputState`（§4.11）, `Player`/`Enemy`/`Shikigami`（§4.13-15）, `castFuda`/`castJu`/`castGenAttack`（G7.4）, `canBeginChinkon`/`applySootheKegare`/`applySubjugateKegare`/`CHINKON_KI_COST`（G7.3）, `ChinkonState`/`CHINKON_CHANNEL_SEC`（§4.7）, `effectiveStats`（§4.3 穢デバフ経路）, `JU_KUSABI`（config）
- Produces（戦闘1フレームを進める統合関数。Game/統合ループが消費。純粋＝Entity副作用のみで Three.js 非依存）:
  ```typescript
  export interface BattleStepInput {
    input: import('../engine/input').InputState;
    dt: number;
    nowMs: number;
  }
  export interface BattleContext {
    player: import('../entities/player').Player;
    gen: import('../entities/shikigami').Shikigami | null;
    enemies: import('../entities/enemy').Enemy[];
    chinkon: import('../combat/chinkon').ChinkonState;
  }
  export interface BattleStepResult {
    chinkon: import('../combat/chinkon').ChinkonState;
    events: BattleEvent[];   // 'fuda'|'ju'|'dodge'|'soothed'|'subjugated'|'chinkon-open'
  }
  export type BattleEvent =
    | { type: 'fuda'; hits: number }
    | { type: 'ju'; hits: number }
    | { type: 'dodge' }
    | { type: 'chinkon-open' }
    | { type: 'chinkon-begin' }
    | { type: 'soothed'; enemyId: number; kegare: number }
    | { type: 'subjugated'; enemyId: number; kegare: number };
  // 入力→アクション解決→鎮魂窓/チャネル進行→討伐検知の1フレーム処理（Entity を破壊的更新）
  export function stepBattle(ctx: BattleContext, step: BattleStepInput): BattleStepResult;
  ```
- Rationale: 「入力エッジ→どのアクションを撃つか」「鎮魂窓が開いたか」「チャネル成功で救済＝穢−」「鎮魂せず死んだら討伐＝穢+」「穢段階デバフが effectiveStats を通じて与/被ダメに乗るか」という**戦闘の意味的判定**は決定的でテスト可能。これを `battle-system.ts` に集約し、見た目（ゲージUI/プロンプト/演出）はG8と本群§G7.7の目視に委ねる。

- [ ] 失敗するテストを書く（完全なテストコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/battle-system.test.ts`:
  ```typescript
  import { describe, it, expect, beforeEach } from 'vitest';
  import { stepBattle, type BattleContext, type BattleStepInput } from '../src/combat-runtime/battle-system';
  import { Player } from '../src/entities/player';
  import { Enemy } from '../src/entities/enemy';
  import { ChinkonState } from '../src/combat/chinkon';
  import { KegareGauge } from '../src/combat/kegare';
  import { SOOTHE_KEGARE_DELTA, SUBJUGATE_KEGARE_DELTA } from '../src/combat-runtime/chinkon-flow';
  import type { Stats } from '../src/combat/stats';
  import type { InputState } from '../src/engine/input';

  const stats = (over: Partial<Stats> = {}): Stats => ({
    hp: 100, ki: 50, tai: 80, go: 0, jutsuryoku: 90, spd: 0,
    critRate: 0, critMul: 1.5, ...over,
  });
  const noInput = (over: Partial<InputState> = {}): InputState => ({
    moveX: 0, moveZ: 0, fuda: false, ju1: false, ju2: false,
    dodge: false, summon: false, switchShiki: false, item: false, chinkon: false, ...over,
  });
  function makePlayer(): Player {
    const p = new Player({ x: 0, z: 0, element: 'water', baseStats: stats(), radius: 0.4 });
    p.facing = 'right'; p.ki = 50; p.maxKi = 50; p.kegare = new KegareGauge(0);
    return p;
  }
  function makeEnemy(hp = 100, miren = true): Enemy {
    const e = new Enemy({ x: 3, z: 0, element: 'fire', baseStats: stats({ hp }), radius: 0.5 });
    e.hp = hp; e.maxHp = 100; e.hasMiren = miren;
    return e;
  }
  const step = (over: Partial<BattleStepInput> = {}): BattleStepInput => ({
    input: noInput(), dt: 1 / 60, nowMs: 1000, ...over,
  });

  describe('stepBattle', () => {
    let ctx: BattleContext;
    beforeEach(() => {
      ctx = { player: makePlayer(), gen: null, enemies: [makeEnemy()], chinkon: new ChinkonState() };
    });

    it('符入力で前方の敵にダメージ＋fudaイベント', () => {
      const before = ctx.enemies[0].hp;
      const r = stepBattle(ctx, step({ input: noInput({ fuda: true }) }));
      expect(ctx.enemies[0].hp).toBeLessThan(before);
      expect(r.events.some((e) => e.type === 'fuda')).toBe(true);
    });

    it('呪入力で気消費＋juイベント', () => {
      const beforeKi = ctx.player.ki;
      const r = stepBattle(ctx, step({ input: noInput({ ju1: true }) }));
      expect(ctx.player.ki).toBeLessThan(beforeKi);
      expect(r.events.some((e) => e.type === 'ju')).toBe(true);
    });

    it('敵HP30%以下＋未練ありで鎮魂窓が開く（chinkon-open）', () => {
      ctx.enemies[0].hp = 25; // 25%
      const r = stepBattle(ctx, step());
      expect(r.chinkon.phase).toBe('open');
      expect(r.events.some((e) => e.type === 'chinkon-open')).toBe(true);
    });

    it('鎮魂入力でチャネル開始→1.5s経過で救済（穢−15・敵昇華）', () => {
      ctx.enemies[0].hp = 20;
      // 窓を開く
      let r = stepBattle(ctx, step());
      ctx.chinkon = r.chinkon;
      // 鎮魂キーでチャネル開始
      r = stepBattle(ctx, step({ input: noInput({ chinkon: true }) }));
      ctx.chinkon = r.chinkon;
      expect(r.chinkon.phase).toBe('channeling');
      // 1.5s ぶん進める
      r = stepBattle(ctx, step({ dt: 1.5 }));
      expect(r.events.some((e) => e.type === 'soothed')).toBe(true);
      expect(ctx.player.kegare.value).toBe(Math.max(0, 0 + SOOTHE_KEGARE_DELTA)); // 0→クランプ0
      expect(ctx.enemies[0].alive).toBe(false); // 救済＝退場
    });

    it('鎮魂せず討伐すると穢が+7される（subjugated）', () => {
      ctx.player.kegare = new KegareGauge(50);
      ctx.enemies[0].hp = 1; // 符1発で死ぬ想定
      const r = stepBattle(ctx, step({ input: noInput({ fuda: true }) }));
      // 敵が死んだ かつ 鎮魂していない → 討伐
      expect(ctx.enemies[0].alive).toBe(false);
      expect(r.events.some((e) => e.type === 'subjugated')).toBe(true);
      expect(ctx.player.kegare.value).toBe(50 + SUBJUGATE_KEGARE_DELTA); // 57
    });

    it('未練なし敵では鎮魂窓が開かない', () => {
      ctx.enemies = [makeEnemy(20, false)];
      const r = stepBattle(ctx, step());
      expect(r.chinkon.phase).toBe('closed');
    });
  });

  describe('穢デバフの被ダメ反映', () => {
    it('重い穢(70+)で被攻撃側の護が下がりダメージが増える', async () => {
      const { effectiveStats } = await import('../src/combat/stats');
      const base = stats({ go: 100 });
      const normal = effectiveStats(base, undefined, 0);   // 平常
      const heavy = effectiveStats(base, undefined, 75);   // 重い穢→護×0.85
      expect(heavy.go).toBeLessThan(normal.go);
      expect(heavy.go).toBe(Math.floor(100 * 0.85)); // 85（契約: 重い穢で体・護×0.85）
    });
  });
  ```
- [ ] テスト実行で失敗を確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/battle-system.test.ts`。期待出力: `Failed to load url ../src/combat-runtime/battle-system` で **FAIL**。
- [ ] 最小実装（完全なコード）。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/combat-runtime/battle-system.ts`:
  ```typescript
  // module: combat-runtime/battle-system | 戦闘1フレーム統合（入力→アクション→鎮魂→討伐検知）
  // Three.js 非依存。Entity を破壊的更新し、UI/演出が読むイベント配列を返す。
  import type { InputState } from '../engine/input';
  import type { Player } from '../entities/player';
  import type { Shikigami } from '../entities/shikigami';
  import type { Enemy } from '../entities/enemy';
  import { ChinkonState } from '../combat/chinkon';
  import { castFuda, castJu, castGenAttack } from './actions';
  import { JU_KUSABI } from '../config';
  import {
    canBeginChinkon,
    applySootheKegare,
    applySubjugateKegare,
    CHINKON_KI_COST,
  } from './chinkon-flow';

  export interface BattleStepInput {
    input: InputState;
    dt: number;
    nowMs: number;
  }
  export interface BattleContext {
    player: Player;
    gen: Shikigami | null;
    enemies: Enemy[];
    chinkon: ChinkonState;
  }
  export type BattleEvent =
    | { type: 'fuda'; hits: number }
    | { type: 'ju'; hits: number }
    | { type: 'dodge' }
    | { type: 'chinkon-open' }
    | { type: 'chinkon-begin' }
    | { type: 'soothed'; enemyId: number; kegare: number }
    | { type: 'subjugated'; enemyId: number; kegare: number };
  export interface BattleStepResult {
    chinkon: ChinkonState;
    events: BattleEvent[];
  }

  // 鎮魂対象の代表敵（窓が開く最初の生存敵）を選ぶ
  function chinkonTarget(enemies: readonly Enemy[]): Enemy | null {
    for (const e of enemies) {
      if (e.alive && e.chinkonWindowOpen()) return e;
    }
    return null;
  }

  export function stepBattle(ctx: BattleContext, step: BattleStepInput): BattleStepResult {
    const { input, dt, nowMs } = step;
    const events: BattleEvent[] = [];
    let chinkon = ctx.chinkon;

    // 死亡前HPを記録（このフレームの攻撃で討伐されたか後で判定するため）
    const aliveBefore = new Map<number, boolean>();
    for (const e of ctx.enemies) aliveBefore.set(e.id, e.alive);

    // --- 回避 ---
    if (input.dodge && ctx.player.canDodge(nowMs)) {
      ctx.player.startDodge(nowMs);
      events.push({ type: 'dodge' });
    }

    // --- 攻撃（チャネル中は無防備＝攻撃不可） ---
    const channeling = chinkon.phase === 'channeling';
    if (!channeling) {
      if (input.fuda) {
        const r = castFuda(ctx.player, ctx.enemies);
        events.push({ type: 'fuda', hits: r.hits.length });
      }
      if (input.ju1 && ctx.player.canCastJu(nowMs)) {
        const r = castJu(ctx.player, ctx.enemies, {
          skillMul: JU_KUSABI.skillMul, kiCost: JU_KUSABI.kiCost,
          arcDeg: JU_KUSABI.arcDeg, range: JU_KUSABI.range,
        });
        if (r.hits.length > 0 || ctx.player.ki >= 0) ctx.player.markJuCast(nowMs);
        events.push({ type: 'ju', hits: r.hits.length });
      }
      if (ctx.gen && ctx.gen.summoned && ctx.gen.intent === 'attack') {
        castGenAttack(ctx.gen, ctx.enemies);
      }
    }

    // --- 鎮魂窓・チャネル進行 ---
    const target = chinkonTarget(ctx.enemies);
    if (chinkon.phase === 'closed' && target) {
      chinkon = chinkon.open();
      events.push({ type: 'chinkon-open' });
    } else if (chinkon.phase === 'open' && !target) {
      // 窓が閉じた（対象が消えた/HP回復）→ closed へ戻す扱い（cancelで畳む）
      chinkon = chinkon.cancel();
    }

    if (chinkon.phase === 'open' && input.chinkon
        && target && canBeginChinkon(target.hp, target.maxHp, target.hasMiren, ctx.player.ki)) {
      ctx.player.ki -= CHINKON_KI_COST;
      chinkon = chinkon.beginChannel();
      events.push({ type: 'chinkon-begin' });
    }

    if (chinkon.phase === 'channeling') {
      // 被弾はGame側で player.takeDamageAt → 中断は外部から chinkon.cancel される想定。
      chinkon = chinkon.tick(dt);
      if (chinkon.phase === 'succeeded' && target) {
        ctx.player.kegare = applySootheKegare(ctx.player.kegare);
        target.alive = false; // 救済＝昇華退場（討伐扱いにしない）
        events.push({ type: 'soothed', enemyId: target.id, kegare: ctx.player.kegare.value });
        chinkon = new ChinkonState(); // closed へリセット
      }
    }

    // --- 討伐検知（このフレームで生存→死亡に転じ、鎮魂で退場でない敵） ---
    const soothedIds = new Set(
      events.filter((e): e is Extract<BattleEvent, { type: 'soothed' }> => e.type === 'soothed')
        .map((e) => e.enemyId),
    );
    for (const e of ctx.enemies) {
      const was = aliveBefore.get(e.id) ?? false;
      if (was && !e.alive && !soothedIds.has(e.id)) {
        if (e.hasMiren) {
          ctx.player.kegare = applySubjugateKegare(ctx.player.kegare);
          events.push({ type: 'subjugated', enemyId: e.id, kegare: ctx.player.kegare.value });
        }
      }
    }

    return { chinkon, events };
  }
  ```
  > 実装注: 穢デバフの**与/被ダメ反映**は `effectiveStats(base, equip, kegareLevel)` 経由で一元化済み（契約 §4.3）。`actions.ts` は `effectiveStats(e.baseStats)` を護に使うため、被攻撃側に穢段階を渡す統合は G8/Game でエネミーの穢を持たせる際に `effectiveStats(e.baseStats, undefined, e.kegareLevel)` へ差し替える（本群テストは effectiveStats の穢分岐自体を検証し、経路の正しさを保証する）。Phase0 では主人公の穢が主で、敵穢は未使用。チャネル中断（被弾）は Game ループが `chinkon.cancel()` を呼ぶ契約とし、本関数は cancel 済み state を受け取れば channeling 分岐に入らない。
- [ ] テスト実行で PASS 確認: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/battle-system.test.ts`。期待出力: 全ケース緑。
- [ ] フルスイート回帰: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run`。期待出力: 全テスト緑。
- [ ] コミット: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add src/combat-runtime/battle-system.ts test/battle-system.test.ts && git commit -m "feat(G7): battle-system 入力→アクション→鎮魂→討伐の統合（TDD）"`

---

### Task G7.7: 戦闘の手触り目視検証（符/呪/玄攻撃/回避/鎮魂/穢デバフ）

**Files**
- Modify: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/world/scene-test.ts`（戦闘配線の組み込み・敵を未練持ちで配置。G6 で雛形がある前提で `stepBattle` 呼び出しを追加）
- Modify: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/game.ts`（update 内で被弾時 `player.takeDamageAt(dmg, nowMs)` とチャネル被弾時 `chinkon.cancel()` を配線）
- 検証成果物（コミット対象外の確認手順）: `tools/screenshot.mjs`（G8提供。本タスクでは既存を流用）

**Interfaces**
- Consumes: `stepBattle`/`BattleContext`（G7.6）, `Player.takeDamageAt`（G7.5）, `ChinkonState.cancel`（§4.7）, `Hud`/`ChinkonPrompt`（§4.18・G8提供。未完なら本タスクは Hud なしで挙動のみ確認）
- Produces: 戦闘が画面上で機能する配線。TDD不可（WebGL・実時間・視覚/手触り判断）のため目視チェックリストで合否を取る。

このタスクは描画/手触りのため TDD ではなく「実装→headless スクショ＋手動チェックリスト目視→コミット」で進める。

- [ ] 実装（完全なコード）。`game.ts` の update ループに戦闘配線を追加する。`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/game.ts` の更新処理に以下の戦闘ステップ呼び出しを組み込む（既存の入力サンプル・エンティティ更新・描画は保持。`battleCtx` はシーン初期化で生成済みとする）:
  ```typescript
  // game.ts update(dt) 内：入力サンプル後・描画前に挿入
  // this.battleCtx: BattleContext（scene-test で初期化）、this.input: Input、this.nowMs: 経過ms
  const inputState = this.input.sample();
  const result = stepBattle(this.battleCtx, {
    input: inputState,
    dt,
    nowMs: this.nowMs,
  });
  this.battleCtx.chinkon = result.chinkon;

  // 敵→主人公の被弾はここで適用（i-frame をtakeDamageAtに委譲）
  for (const e of this.battleCtx.enemies) {
    if (e.alive && e.aiState === 'attack' && e.justLanded) {
      // チャネル中に被弾したら鎮魂中断
      if (this.battleCtx.chinkon.phase === 'channeling') {
        this.battleCtx.chinkon = this.battleCtx.chinkon.cancel();
      }
      this.battleCtx.player.takeDamageAt(e.attackPower, this.nowMs);
      e.justLanded = false;
    }
  }

  // HUD 反映（G8 Hud がある場合）
  if (this.hud) {
    this.hud.setHp(this.battleCtx.player.hp, this.battleCtx.player.maxHp);
    this.hud.setKi(this.battleCtx.player.ki, this.battleCtx.player.maxKi);
    this.hud.setKegare(this.battleCtx.player.kegare.value);
  }
  // 鎮魂プロンプト
  if (this.chinkonPrompt) {
    const phase = this.battleCtx.chinkon.phase;
    if (phase === 'open' || phase === 'channeling') {
      this.chinkonPrompt.show();
      this.chinkonPrompt.setProgress(this.battleCtx.chinkon.progress);
    } else {
      this.chinkonPrompt.hide();
    }
  }
  ```
  > 注: `e.justLanded`/`e.attackPower` は G6 の `Enemy` 攻撃ステートで設定される想定フィールド。G6 で別名なら G6 側の実フィールド名に合わせて読み替える（本タスクは G6 の敵攻撃出力を被弾に接続するのが目的）。`battleCtx`/`hud`/`chinkonPrompt` は `Game` のフィールドとして scene-test 初期化時に組む。
- [ ] dev サーバ起動: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npm run dev`（別ターミナルで起動し http://localhost:5173 を開く）。
- [ ] headless スクショ撮影: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && node tools/screenshot.mjs`（既定状態・符発動中・呪発動中・回避中・鎮魂窓表示・チャネル中の各シーンを `tools/shots/` 等へ出力）。
- [ ] 手動チェックリスト目視（各項目 pass/fail を記録。全 pass で合格）:
  1. **符**: J キーで前方に符が飛び、射程内の鬼火童子の HP バーが減る。背後の敵には当たらない。
  2. **呪**: K キーで気バーが `JU_KUSABI.kiCost` ぶん減り、絞った前方扇でダメージが入る。気不足時は何も起きず気が減らない。CD 中（約2.5s）は再発動しない。
  3. **玄の攻撃**: 玄が敵に近接した際に疾風斬が出て敵 HP が減る（玄が `intent='attack'` の時のみ）。
  4. **回避 i-frame**: Space で凪沙が前方へ短距離ダッシュし、無敵窓（発生0.05〜0.28s）中に敵攻撃が重なっても HP が減らない。窓外の被弾では減る。CD 0.6s 中は再回避できない。
  5. **鎮魂窓**: 鬼火童子（未練あり）の HP を 30% 以下まで削ると鎮魂プロンプトが表示される（`ChinkonPrompt.show`）。
  6. **チャネル**: 鎮魂キーで気が `CHINKON_KI_COST`(20) 減り、約1.5s のゲージが伸びる。チャネル中は符/呪が出ず無防備。完走で敵が昇華退場し、穢ゲージが下がる（救済 −15）。
  7. **チャネル中断**: チャネル中に敵攻撃を受けると鎮魂が中断（プロンプトが消える）。
  8. **討伐で穢増**: 鎮魂せず未練持ち敵を倒すと穢ゲージが上がる（討伐 +7）。
  9. **穢段階デバフ**: 穢を 40 以上（軽い穢）にすると気回復が鈍り、70 以上（重い穢）で凪沙の被ダメが増える（護低下の体感）。HUD の穢ゲージ色/段階表示が閾値 40/70/100 で変わる。
  10. **HUD**: HP/気/穢バーがスクロールバー無しの固定枠（UIPanel思想）で減少・回復に追従する。
- [ ] スクショ拡大確認: 符/呪の扇方向が `facing` と一致し、右向き時に flipX で正しく前方へ判定が出ているか（座標と向きの不整合が無いか）を目視。
- [ ] dev サーバ停止後コミット: `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add src/game.ts src/world/scene-test.ts && git commit -m "feat(G7): 戦闘統合の実機配線＋手触り目視検証（符/呪/玄/回避/鎮魂/穢）"`

---

### G7 完了条件（Definition of Done）

- [ ] 純粋ロジック（`hitbox.ts`/`resolve.ts`/`chinkon-flow.ts`/`actions.ts`/`battle-system.ts`/`player.ts` の i-frame）が Vitest で全緑、かつ `combat/` への逆依存ゼロ・Three.js import ゼロ（`actions.ts`/`battle-system.ts`/`player.ts` は entities を介すが Three.js は import しない）。
- [ ] `npx vitest run` フルスイートが G5+G7 通しで緑（既存破壊なし）。
- [ ] 符（気消費なし・前方扇・skillMul=1.0）／呪（気消費・術力基礎・CD）／玄攻撃／回避 i-frame（距離2.5・無敵0.05〜0.28s・CD0.6s）が入力で機能（目視 §G7.7 1-4 pass）。
- [ ] 未練持ち敵 HP30%以下で鎮魂窓→鎮魂入力で約1.5sチャネル→成功＝救済（穢−15）／討伐＝穢+7（目視 §G7.7 5-8 pass、ロジック §G7.3/G7.6 緑）。
- [ ] 穢段階デバフ（軽い穢で気回復−25%・重い穢で体護×0.85）が `effectiveStats` 経由で戦闘に反映（§G7.6 穢分岐テスト緑＋目視 9 pass）。
- [ ] 型・関数名・パスが契約 §4.8/§4.17 に逐語一致（`circleHit`/`fanHit`/`rectHit`/`ActionResult`/`castFuda`/`castJu`/`castGenAttack`）。新規追加（`resolve.ts`/`chinkon-flow.ts`/`battle-system.ts`）は `combat-runtime/` 配下に置き純粋層を片方向 import。

---

**G7 起草完了**。契約 §4.8（hitbox）・§4.17（actions）を逐語実装する7タスクで構成した。要点:

- **純粋核を最大化**: 契約の `hitbox.ts`（§4.8）に加え、判定→ダメージ集計を `resolve.ts`、鎮魂の穢変動を `chinkon-flow.ts` に切り出して Vitest TDD 可能にした（いずれも `combat-runtime/` 配下・`combat/` を片方向 import）。
- **契約逐語**: `ActionResult`/`castFuda`/`castJu`/`castGenAttack`（§4.17）、`Player.isInvincible`/`iFrameUntilMs`/`kegare`（§4.13）、`Enemy.chinkonWindowOpen`/`hasMiren`（§4.15）をそのまま使用。
- **数値は契約/00b 由来**: i-frame（距離2.5・無敵0.05〜0.28s・CD0.6s）、鎮魂（HP30%・1.5s・気20）、穢（救済−15／討伐+7＝00b の −10〜20／+5〜10 の代表値・段階40/70/100）、ダメージ例題（体100×100/(100+100)=50 等）を逐語固定。
- **手触りは目視**: 符/呪/玄/回避/鎮魂/穢デバフの実機挙動は WebGL・実時間判断のため §G7.7 の headless スクショ＋10項目チェックリストで検証。
- **契約への新規追加点（後続が衝突しうる箇所）**: ① `resolve.ts`/`chinkon-flow.ts`/`battle-system.ts` を `combat-runtime/` に追加（契約 File Structure に無い純核ヘルパ。`combat/` 不可侵を維持）。② `config.ts` に `FUDA`/`JU_KUSABI`/`GEN_ATTACK`/`DODGE` 定数を追加。③ `Player` に `startDodge`/`canDodge`/`takeDamageAt`/`canCastJu`/`markJuCast` を追加（契約 §4.13 の既存シグネチャは保持）。④ 被弾適用は `takeDamageAt(amount, nowMs)` を使う契約をG8/統合へ申し送り。これらは G8/統合担当へ要連携。
