<!-- G1 プロジェクト基盤・ビルド | phase0 plan | 2026-06-17 -->
全ての裏取りが完了した。G1タスク群のmarkdownを起草する。

G1は「画面が出る」までの基盤なので、純粋ロジックのTDDサイクルは適用しづらいが、スモークテストとして「ビルド成功」「型チェック成功」「Vitestが動く（プレースホルダ純テスト）」「dev起動で#appにcanvas」を機械検証可能なステップに落とし込む。Three.jsを使う最初のスモーク描画は headless スクショ検証にする。

---

# G1 プロジェクト基盤・ビルド

> 依存: なし（最初の群）。後続 G2〜G8 が乗る土台。
> 成果物: `package.json` / `tsconfig.json` / `vite.config.ts`（`base:'./'`）/ `index.html` / `src` 雛形＋dev/build/test/screenshot スクリプト、Three.js r0.171.0 固定の lockfile、「画面が出る」スモーク。
> 上位権威: `phase0-shared-contract.md` §1.3 Tech Stack ＞ §3 File Structure。型・パス・シグネチャはここに逐語一致。
> 作業場所: `~/projects/onmyoji-rpg/`（非iCloud・git破壊回避）。環境: Node v23.11.0 / npm 10.9.2。

---

### Task G1.1: リポジトリ初期化と .gitignore

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/.gitignore`
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/.nvmrc`

**Interfaces**
- Consumes: なし
- Produces: git リポジトリ（`onmyoji-rpg/` ルート）・無視設定。`docs/bible/` は既存のまま温存。

このタスクは基盤の器。コードを変えるステップが無いため TDD サイクルではなく、検証は「git status と ls で初期状態を機械確認」で行う。

- [ ] git 未初期化を確認する（コマンドと期待出力）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git rev-parse --is-inside-work-tree 2>&1 | head -1
```
期待出力: `fatal: not a git repository (or any of the parent directories): .git`（まだ未初期化）。

- [ ] git リポジトリを初期化する

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git init -b main && git config user.name "onmyoji-rpg" && git config user.email "nyaledge.capital1983@gmail.com"
```

- [ ] `.gitignore` を作成する（完全な内容）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/.gitignore`:
```gitignore
# deps
node_modules/

# build
dist/
.vite/

# test / coverage
coverage/
test-results/
playwright-report/
.playwright/

# screenshots (検証ハーネス出力。lockfile は無視しない)
tools/shots/

# editor / os
.DS_Store
*.log
npm-debug.log*
.idea/
.vscode/

# env
.env
.env.*
```

- [ ] `.nvmrc` を作成する（完全な内容）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/.nvmrc`:
```
23
```

- [ ] 初期状態を機械確認する（コマンドと期待出力）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git rev-parse --is-inside-work-tree && ls -a | grep -E "^\.gitignore$|^\.nvmrc$|^docs$"
```
期待出力（順不同で3行＋`true`）: `true` / `.gitignore` / `.nvmrc` / `docs`。

- [ ] コミットする

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add .gitignore .nvmrc && git commit -m "chore: init repo with gitignore and nvmrc"
```

---

### Task G1.2: package.json（依存固定・npm スクリプト）

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/package.json`
- Test: `package-lock.json` の生成（`npm install` 後にコミット対象）

**Interfaces**
- Consumes: なし
- Produces: 依存固定（Three.js `^0.171.0` ＋ `@types/three@^0.171.0`・Vite `^6.0.0`・TypeScript `^5.6.0`・Vitest `^2.1.0`・Playwright `^1.49.0`）。スクリプト `dev` / `build` / `preview` / `typecheck` / `test` / `test:run` / `coverage` / `screenshot`。

依存の固定は契約 §1.3 に逐語一致させる。検証は「`npm install` 成功 → lockfile に three の解決バージョンが入る」を機械確認。

- [ ] `package.json` を作成する（完全な内容）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/package.json`:
```json
{
  "name": "onmyoji-rpg",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "京音都・陰陽師アクションRPG Phase0 技術プロト",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest",
    "test:run": "vitest run",
    "coverage": "vitest run --coverage",
    "screenshot": "node tools/screenshot.mjs"
  },
  "dependencies": {
    "three": "^0.171.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.49.0",
    "@types/three": "^0.171.0",
    "@vitest/coverage-v8": "^2.1.0",
    "typescript": "^5.6.0",
    "vite": "^6.0.0",
    "vitest": "^2.1.0"
  }
}
```

- [ ] 依存をインストールし lockfile を生成する（コマンド）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npm install
```

- [ ] Three.js が r0.171.x に解決されたことを機械確認する（コマンドと期待出力）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npm ls three --depth=0
```
期待出力: `└── three@0.171.0`（マイナーが 171 でなければ FAIL）。
> 注（2026-06-17 実装時修正）: 当初の `node -e "require('three/package.json')"` は three の package exports が `./package.json` を公開しないため `ERR_PACKAGE_PATH_NOT_EXPORTED` で動かない。`npm ls` で確認する。

- [ ] Playwright の Chromium バイナリを取得する（コマンド）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx playwright install chromium
```

- [ ] コミットする（lockfile を必ず含める）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add package.json package-lock.json && git commit -m "build: pin deps (three r0.171.0, vite6, ts5, vitest2, playwright1) with lockfile"
```

---

### Task G1.3: tsconfig.json（strict TS）

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/tsconfig.json`
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/tsconfig.node.json`

**Interfaces**
- Consumes: なし
- Produces: TypeScript 5.x strict 設定（`strict: true`・`moduleResolution: bundler`・`noEmit`）。`src/`・`test/`・`tools/` を含む。

検証は「strict 違反コードが `tsc --noEmit` で落ちる → 正しいコードで通る」で TDD 風に行う（型チェックがゲートとして機能することの確認）。

- [ ] `tsconfig.json` を作成する（完全な内容）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/tsconfig.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022", "DOM", "DOM.Iterable"],
    "types": ["three"],
    "strict": true,
    "noUnusedLocals": true,
    "noUnusedParameters": true,
    "noImplicitReturns": true,
    "noFallthroughCasesInSwitch": true,
    "noUncheckedIndexedAccess": true,
    "exactOptionalPropertyTypes": false,
    "forceConsistentCasingInFileNames": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "resolveJsonModule": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "noEmit": true
  },
  "include": ["src", "test"]
}
```
> 注（2026-06-17 実装時修正）: 当初あった `"references": [{ "path": "./tsconfig.node.json" }]` は外した。プロジェクト参照は参照先に `composite: true` を要求し `noEmit` を禁じるため `tsc --noEmit` が TS6306/TS6310 で落ちる。アプリ型チェックに参照は不要（vite.config 等は vite が自前で処理）。`tsconfig.node.json` はツール設定の記録として単体で残す。

- [ ] `tsconfig.node.json` を作成する（vite.config / tools 用・完全な内容）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/tsconfig.node.json`:
```json
{
  "compilerOptions": {
    "target": "ES2022",
    "module": "ESNext",
    "moduleResolution": "bundler",
    "lib": ["ES2022"],
    "strict": true,
    "esModuleInterop": true,
    "skipLibCheck": true,
    "isolatedModules": true,
    "verbatimModuleSyntax": true,
    "noEmit": true
  },
  "include": ["vite.config.ts", "vitest.config.ts", "playwright.config.ts", "tools"]
}
```

- [ ] strict ゲートが機能することを確認する一時ファイルを作る（失敗を意図的に起こす）

一時ファイル `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/__strict_probe.ts` を作成（完全な内容・型エラーを含む）:
```typescript
// strict 検証用の一時プローブ。implicit any と未使用変数で必ず落ちる想定
export function probe(value): number {
  const unused = 1;
  return value;
}
```

- [ ] 型チェックで失敗を確認する（コマンドと期待出力 FAIL）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx tsc --noEmit 2>&1 | grep -E "error TS7006|error TS6133" | head -3
```
期待出力（FAIL を示す）: `src/__strict_probe.ts ... error TS7006: Parameter 'value' implicitly has an 'any' type.` と `error TS6133: 'unused' is declared but its value is never read.` の少なくとも2行が出る。

- [ ] プローブを削除し strict が通る状態に戻す（最小実装＝プローブ除去）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && rm src/__strict_probe.ts
```

- [ ] 型チェックが通ることを確認する（コマンドと期待出力 PASS）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx tsc --noEmit && echo "TYPECHECK_OK"
```
期待出力: エラー無しで終了し最終行に `TYPECHECK_OK`（この時点で `src/` は次タスク G1.4 の雛形作成前なので空。`include` に該当ファイルが無くても tsc は成功する）。

- [ ] コミットする

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add tsconfig.json tsconfig.node.json && git commit -m "build: add strict TypeScript config (ts5, bundler resolution)"
```

---

### Task G1.4: src 雛形（types / config / 各層プレースホルダ）

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/types.ts`
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/config.ts`
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/main.ts`
- Test: `tsc --noEmit` で雛形全体が型として整合すること

**Interfaces**
- Consumes: なし
- Produces（契約 §4.1 / §2 に逐語一致）:
```typescript
// src/types.ts
export type Element = 'wood' | 'fire' | 'earth' | 'metal' | 'water';
export type Facing = 'down' | 'left' | 'right' | 'up';
export type SceneState = 'title' | 'field' | 'battle' | 'menu' | 'event' | 'gameover';
export interface Vec2 { x: number; z: number; }
export interface Rect { x: number; z: number; w: number; h: number; }
```
`src/config.ts` は契約 §2 Global Constraints の全数値の集約点。

G1 では `types.ts` / `config.ts` の確定値を機械検証する（後続群が逐語参照する正典のため）。`main.ts` はスモークの土台で G1.6 まで仮実装。

- [ ] 失敗するテストを書く（完全なテストコード）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/test/config.test.ts`:
```typescript
import { describe, it, expect } from 'vitest';
import { CONFIG } from '../src/config';

describe('CONFIG（契約 §2 Global Constraints の数値集約点）', () => {
  it('カメラ角は yaw45 / pitch30 固定', () => {
    expect(CONFIG.camera.yawDeg).toBe(45);
    expect(CONFIG.camera.pitchDeg).toBe(30);
  });

  it('内部解像度は 1280x720', () => {
    expect(CONFIG.internalWidth).toBe(1280);
    expect(CONFIG.internalHeight).toBe(720);
  });

  it('fog は藍墨 #10131c / near12 / far40・near/far クリップ -1000/1000', () => {
    expect(CONFIG.fog.color).toBe(0x10131c);
    expect(CONFIG.fog.near).toBe(12);
    expect(CONFIG.fog.far).toBe(40);
    expect(CONFIG.camera.clipNear).toBe(-1000);
    expect(CONFIG.camera.clipFar).toBe(1000);
  });

  it('移動速度 baseMoveSpeed = 4.0 unit/s', () => {
    expect(CONFIG.move.baseMoveSpeed).toBe(4.0);
  });

  it('i-frame 距離2.5 / 全体0.45s / 無敵0.05〜0.28s / CD0.6s', () => {
    expect(CONFIG.iframe.dashDistance).toBe(2.5);
    expect(CONFIG.iframe.totalSec).toBe(0.45);
    expect(CONFIG.iframe.invincibleStartSec).toBe(0.05);
    expect(CONFIG.iframe.invincibleEndSec).toBe(0.28);
    expect(CONFIG.iframe.cooldownSec).toBe(0.6);
  });

  it('五行倍率 相生0.85 / 相克1.25 / 無関係1.0', () => {
    expect(CONFIG.element.shoseiMul).toBe(0.85);
    expect(CONFIG.element.sokokuMul).toBe(1.25);
    expect(CONFIG.element.neutralMul).toBe(1.0);
  });

  it('穢の閾値 light40 / heavy70 / berserk100', () => {
    expect(CONFIG.kegare.light).toBe(40);
    expect(CONFIG.kegare.heavy).toBe(70);
    expect(CONFIG.kegare.berserk).toBe(100);
  });

  it('鎮魂 HP30%窓 / チャネル1.5s', () => {
    expect(CONFIG.chinkon.hpRatio).toBe(0.30);
    expect(CONFIG.chinkon.channelSec).toBe(1.5);
  });

  it('ビルボード既定板サイズ 1 x 1.8 / 足元アンカー', () => {
    expect(CONFIG.billboard.defaultWidth).toBe(1.0);
    expect(CONFIG.billboard.defaultHeight).toBe(1.8);
  });
});
```

- [ ] テスト実行で失敗を確認する（コマンドと期待出力 FAIL）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/config.test.ts 2>&1 | tail -15
```
期待出力（FAIL）: `Failed to resolve import "../src/config"` または `Cannot find module` を含み、テストが失敗する（`config.ts` 未作成のため）。

- [ ] 最小実装する：`src/types.ts`（完全なコード・契約 §4.1 逐語）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/types.ts`:
```typescript
// 共有型（横断型のみ）。契約 §4.1 に逐語一致。
export type Element = 'wood' | 'fire' | 'earth' | 'metal' | 'water';
export type Facing = 'down' | 'left' | 'right' | 'up';
export type SceneState = 'title' | 'field' | 'battle' | 'menu' | 'event' | 'gameover';

// XZ平面（地表）座標。y は描画専用で判定に使わない
export interface Vec2 {
  x: number;
  z: number;
}

// 中心(x,z)・幅w・奥行h の矩形（XZ平面）
export interface Rect {
  x: number;
  z: number;
  w: number;
  h: number;
}
```

- [ ] 最小実装する：`src/config.ts`（完全なコード・契約 §2 の全数値を集約）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/config.ts`:
```typescript
// 全定数の集約点。契約 §2 Global Constraints の数値をここに一元化する。
// 後続群（camera / move / damage / kegare / chinkon / billboard）はここを参照する。
export const CONFIG = {
  // 内部解像度（CSS等比フィット）
  internalWidth: 1280,
  internalHeight: 720,

  // カメラ（オルソ・yaw45/pitch30固定・回転しない）
  camera: {
    yawDeg: 45,
    pitchDeg: 30,
    clipNear: -1000,
    clipFar: 1000,
    // 画面に収めるワールド幅（unit）。後続 G2 IsoCamera が viewWorldWidth に使う
    viewWorldWidth: 18,
    // 追従デッドゾーン半径（unit）
    deadZone: 1.5,
    // 追従lerp係数（0..1・フレーム毎）
    followLerp: 0.12,
  },

  // fog（藍墨）と環境光
  fog: {
    color: 0x10131c,
    near: 12,
    far: 40,
  },

  // 移動（A2-1 / B1.2）
  move: {
    baseMoveSpeed: 4.0, // unit/s
  },

  // 回避 i-frame（B1.2）
  iframe: {
    dashDistance: 2.5,        // unit
    totalSec: 0.45,           // 全体
    invincibleStartSec: 0.05, // 無敵開始
    invincibleEndSec: 0.28,   // 無敵終了
    cooldownSec: 0.6,         // クールダウン
  },

  // 五行倍率（00b §3）
  element: {
    shoseiMul: 0.85,  // 相生（不利方向）
    sokokuMul: 1.25,  // 相克（攻撃側有利）
    neutralMul: 1.0,  // 無関係・同属性
  },

  // 穢ゲージ段階閾値（00b §2.3）
  kegare: {
    light: 40,
    heavy: 70,
    berserk: 100,
  },

  // 鎮魂（00b §2.4）
  chinkon: {
    hpRatio: 0.30,    // HP30%以下で窓
    channelSec: 1.5,  // チャネル
  },

  // ビルボード既定（足元アンカー）
  billboard: {
    defaultWidth: 1.0,
    defaultHeight: 1.8,
  },
} as const;
```

- [ ] 最小実装する：`src/main.ts`（スモークの土台・完全なコード）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/main.ts`:
```typescript
// エントリ。G1 スモーク段階では Three.js シーンを #app に描く最小実装（G1.6 で確定）。
// G2 以降で Game クラス（game.ts）へ差し替える。
import { mountSmokeScene } from './engine/smoke';

const app = document.querySelector<HTMLDivElement>('#app');
if (!app) {
  throw new Error('#app マウント点が見つかりません（index.html を確認）');
}
mountSmokeScene(app);
```

- [ ] テスト実行で PASS を確認する（コマンドと期待出力 PASS）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx vitest run test/config.test.ts 2>&1 | tail -8
```
期待出力（PASS）: `Test Files  1 passed (1)` と `Tests  9 passed (9)` を含む。

- [ ] 型チェックが通ることを確認する（コマンドと期待出力）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx tsc --noEmit 2>&1 | grep -v "engine/smoke" | grep "error TS" | head -3; echo "EXIT_CHECK_DONE"
```
期待出力: `engine/smoke` 未作成由来のエラー（`Cannot find module './engine/smoke'`）以外の `error TS` 行が無く、最終行に `EXIT_CHECK_DONE`。この `smoke` モジュールは G1.6 で作成し解消する。

- [ ] コミットする

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add src/types.ts src/config.ts src/main.ts test/config.test.ts && git commit -m "feat: add shared types and CONFIG (contract §2/§4.1 verbatim)"
```

---

### Task G1.5: Vite / Vitest / Playwright 設定（base:'./' ・GitHub Pages 想定）

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/vite.config.ts`
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/vitest.config.ts`
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/playwright.config.ts`
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/index.html`

**Interfaces**
- Consumes: `src/main.ts`（index.html がエントリとして読む）
- Produces: GitHub Pages 向け `base: './'` の Vite 設定・内部解像度 1280×720 を CSS で等比フィットする `index.html`（`#app` マウント点）・Vitest（`environment: node`・coverage v8・純粋ロジック対象）・Playwright（headless Chromium スクショ）。

検証は「ビルド成果物 `dist/index.html` のアセット参照が相対パス（`./assets/...` または相対）になる」を機械確認（GitHub Pages のサブパス配信で壊れないことの担保）。

- [ ] `vite.config.ts` を作成する（完全なコード）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/vite.config.ts`:
```typescript
import { defineConfig } from 'vite';

// GitHub Pages はリポジトリ名サブパス配信になるため base を相対 './' に固定する。
// （絶対 '/' だとサブパスでアセット 404 になる。契約 §1.3 配信）
export default defineConfig({
  base: './',
  server: {
    host: '127.0.0.1',
    port: 5173,
    strictPort: true,
  },
  build: {
    outDir: 'dist',
    target: 'es2022',
    sourcemap: true,
  },
});
```

- [ ] `vitest.config.ts` を作成する（完全なコード）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/vitest.config.ts`:
```typescript
import { defineConfig } from 'vitest/config';

// 純粋ロジック層（src/combat/* ・ combat-runtime/hitbox.ts ・ config）専用。
// 描画層（Three.js）はここで実行せず Playwright スクショで検証する。
export default defineConfig({
  test: {
    environment: 'node',
    include: ['test/**/*.test.ts'],
    coverage: {
      provider: 'v8',
      include: ['src/combat/**', 'src/combat-runtime/hitbox.ts', 'src/config.ts'],
      reporter: ['text', 'html'],
    },
  },
});
```

- [ ] `playwright.config.ts` を作成する（完全なコード）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/playwright.config.ts`:
```typescript
import { defineConfig, devices } from '@playwright/test';

// headless Chromium スクリーンショット検証ハーネスの土台。
// dev サーバ（vite）を webServer として起動し、テストシーンを撮影する。
export default defineConfig({
  testDir: './test-e2e',
  fullyParallel: false,
  workers: 1,
  use: {
    ...devices['Desktop Chrome'],
    headless: true,
    viewport: { width: 1280, height: 720 },
  },
  webServer: {
    command: 'npm run dev',
    url: 'http://127.0.0.1:5173',
    reuseExistingServer: true,
    timeout: 60_000,
  },
});
```

- [ ] `index.html` を作成する（完全なコード・#app マウント点・1280×720 等比フィット）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/index.html`:
```html
<!doctype html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>京音都・陰陽師アクションRPG ─ Phase0 プロト</title>
    <style>
      html,
      body {
        margin: 0;
        padding: 0;
        height: 100%;
        background: #10131c;
        overflow: hidden;
      }
      /* 内部解像度 1280x720 を中央で等比フィット。webスクロールバー厳禁（契約 §2 UI思想） */
      #app {
        position: absolute;
        inset: 0;
        display: flex;
        align-items: center;
        justify-content: center;
      }
      #app canvas {
        display: block;
        max-width: 100%;
        max-height: 100%;
        aspect-ratio: 1280 / 720;
        image-rendering: pixelated;
      }
    </style>
  </head>
  <body>
    <div id="app"></div>
    <script type="module" src="/src/main.ts"></script>
  </body>
</html>
```

- [ ] ビルドはまだ通らないことを確認する（コマンドと期待出力 FAIL）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npm run build 2>&1 | grep -E "Cannot find module|Could not resolve|error" | head -3
```
期待出力（FAIL）: `Could not resolve "./engine/smoke"` を含む（`src/engine/smoke.ts` が G1.6 で作られるまで未解決）。

- [ ] コミットする

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add vite.config.ts vitest.config.ts playwright.config.ts index.html && git commit -m "build: add vite(base ./)/vitest/playwright config and index.html (#app mount, 1280x720 fit)"
```

---

### Task G1.6: 「画面が出る」スモーク（Three.js 最小シーン＋ビルド成功＋スクショ検証）

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/engine/smoke.ts`
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/test-e2e/smoke.spec.ts`
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/tools/screenshot.mjs`
- Modify: なし（`src/main.ts` は G1.4 で既に `mountSmokeScene` を呼ぶ）

**Interfaces**
- Consumes: `three`（r0.171.0）・`CONFIG`（`src/config.ts`）
- Produces:
```typescript
// src/engine/smoke.ts
export function mountSmokeScene(mount: HTMLElement): { dispose(): void };
```
この `smoke.ts` は G2 で `renderer.ts` / `camera.ts` / `loop.ts` に置き換わる暫定スモーク。Three.js が動き「画面に何かが描かれる」ことだけを保証する。

描画は数値検証が困難（WebGL・視覚判断）なため、契約 §5.2 に従い TDD ではなく「実装 → ビルド成功の機械確認 → headless Chromium スクショで目視確認」のステップにする。

- [ ] 実装する：`src/engine/smoke.ts`（完全なコード・Three.js 最小シーン）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/src/engine/smoke.ts`:
```typescript
// G1 スモーク：Three.js が r0.171.0 で動き「画面が出る」ことだけを保証する暫定シーン。
// G2 で renderer.ts / camera.ts / loop.ts に置き換える。
import * as THREE from 'three';
import { CONFIG } from '../config';

export function mountSmokeScene(mount: HTMLElement): { dispose(): void } {
  const renderer = new THREE.WebGLRenderer({ antialias: true });
  renderer.setPixelRatio(1);
  renderer.setSize(CONFIG.internalWidth, CONFIG.internalHeight, false);
  mount.appendChild(renderer.domElement);

  const scene = new THREE.Scene();
  scene.background = new THREE.Color(CONFIG.fog.color);

  // 暫定の透視カメラ（G2 で OrthographicCamera の IsoCamera に置換）
  const camera = new THREE.PerspectiveCamera(
    50,
    CONFIG.internalWidth / CONFIG.internalHeight,
    0.1,
    1000,
  );
  camera.position.set(0, 3, 6);
  camera.lookAt(0, 0.5, 0);

  // 画面に「何かが出る」ことの証拠＝回る箱（朱色）
  const geo = new THREE.BoxGeometry(1, 1, 1);
  const mat = new THREE.MeshStandardMaterial({ color: 0xb83b2e });
  const cube = new THREE.Mesh(geo, mat);
  cube.position.set(0, 0.5, 0);
  scene.add(cube);

  const grid = new THREE.GridHelper(10, 10, 0x4a5066, 0x2a2f40);
  scene.add(grid);

  const ambient = new THREE.AmbientLight(0xffffff, 0.6);
  const dir = new THREE.DirectionalLight(0xffffff, 1.2);
  dir.position.set(3, 5, 2);
  scene.add(ambient, dir);

  let raf = 0;
  let running = true;
  const tick = (): void => {
    if (!running) return;
    cube.rotation.y += 0.01;
    cube.rotation.x += 0.005;
    renderer.render(scene, camera);
    raf = requestAnimationFrame(tick);
  };
  tick();

  return {
    dispose(): void {
      running = false;
      cancelAnimationFrame(raf);
      geo.dispose();
      mat.dispose();
      renderer.dispose();
      if (renderer.domElement.parentNode) {
        renderer.domElement.parentNode.removeChild(renderer.domElement);
      }
    },
  };
}
```

- [ ] 型チェックが完全に通ることを確認する（コマンドと期待出力 PASS）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx tsc --noEmit && echo "TYPECHECK_OK"
```
期待出力: `error TS` 無しで最終行に `TYPECHECK_OK`（G1.4 で残っていた `./engine/smoke` 未解決が解消）。

- [ ] ビルドが成功することを確認する（コマンドと期待出力 PASS）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npm run build 2>&1 | tail -6
```
期待出力（PASS）: `✓ built in` を含み、`dist/index.html` と `dist/assets/*.js` が生成される。エラー行無し。

- [ ] GitHub Pages 想定の相対パス配信を機械確認する（コマンドと期待出力）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && grep -oE 'src="[^"]+"|href="[^"]+"' dist/index.html | grep -E '\./assets|assets/' | head -3
```
期待出力: アセット参照が `./assets/...`（相対）で始まる行が出る。**`/assets/`（絶対パス）で始まる行が出たら FAIL**（`base: './'` が効いていない）。

- [ ] 実装する：`test-e2e/smoke.spec.ts`（完全なコード・Playwright スクショ＋canvas 存在検証）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/test-e2e/smoke.spec.ts`:
```typescript
import { test, expect } from '@playwright/test';

test('スモーク：#app に canvas が出てフレームが進む', async ({ page }) => {
  const errors: string[] = [];
  page.on('pageerror', (e) => errors.push(e.message));
  page.on('console', (msg) => {
    if (msg.type() === 'error') errors.push(msg.text());
  });

  await page.goto('http://127.0.0.1:5173/');

  // #app 配下に canvas が1枚だけ装着される
  const canvas = page.locator('#app canvas');
  await expect(canvas).toHaveCount(1);

  // canvas が内部解像度 1280x720 で生成されている
  const dims = await canvas.evaluate((el) => ({
    w: (el as HTMLCanvasElement).width,
    h: (el as HTMLCanvasElement).height,
  }));
  expect(dims.w).toBe(1280);
  expect(dims.h).toBe(720);

  // WebGL コンテキストが取れている（黒画面=取得失敗 を弾く）
  const hasGl = await canvas.evaluate((el) => {
    const c = el as HTMLCanvasElement;
    return !!(c.getContext('webgl2') || c.getContext('webgl'));
  });
  expect(hasGl).toBe(true);

  // ランタイムエラーが無い
  expect(errors).toEqual([]);

  await page.screenshot({ path: 'tools/shots/smoke.png' });
});
```

- [ ] 実装する：`tools/screenshot.mjs`（完全なコード・検証ハーネス）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/tools/screenshot.mjs`:
```javascript
// 検証ハーネス：dev サーバを起動しテストシーンを headless Chromium で撮影する。
// 契約 §5.2 の「描画/手触りは Playwright スクショで検証」流儀。
// 使い方: npm run screenshot  → tools/shots/scene.png を出力。
import { spawn } from 'node:child_process';
import { mkdir } from 'node:fs/promises';
import { setTimeout as sleep } from 'node:timers/promises';
import { chromium } from '@playwright/test';

const URL = 'http://127.0.0.1:5173/';
const OUT_DIR = 'tools/shots';

async function waitForServer(url, timeoutMs) {
  const deadline = Date.now() + timeoutMs;
  while (Date.now() < deadline) {
    try {
      const res = await fetch(url);
      if (res.ok) return true;
    } catch {
      // サーバ起動待ち
    }
    await sleep(500);
  }
  return false;
}

async function main() {
  await mkdir(OUT_DIR, { recursive: true });

  const dev = spawn('npm', ['run', 'dev'], { stdio: 'ignore' });
  const cleanup = () => {
    if (!dev.killed) dev.kill('SIGTERM');
  };
  process.on('exit', cleanup);

  try {
    const up = await waitForServer(URL, 30_000);
    if (!up) throw new Error('dev サーバが 30s 以内に起動しませんでした');

    const browser = await chromium.launch({ headless: true });
    const page = await browser.newPage({ viewport: { width: 1280, height: 720 } });
    await page.goto(URL, { waitUntil: 'networkidle' });
    // 数フレーム描画させてから撮影
    await sleep(1000);
    await page.screenshot({ path: `${OUT_DIR}/scene.png` });
    await browser.close();
    console.log(`screenshot saved: ${OUT_DIR}/scene.png`);
  } finally {
    cleanup();
  }
}

main().catch((err) => {
  console.error(err);
  process.exit(1);
});
```

- [ ] スクショ検証を実行し合格条件を満たすことを確認する（コマンドと期待出力 PASS）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npx playwright test 2>&1 | tail -6
```
期待出力（PASS）: `1 passed` を含む。`tools/shots/smoke.png` が生成される。

- [ ] スクショの目視チェック（具体手順＋合格条件）

手順:
1. `cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npm run screenshot` を実行する。
2. 出力された `tools/shots/scene.png` を開く（macOS は `open tools/shots/scene.png`）。

合格条件（すべて満たすこと）:
- 背景が藍墨色（`#10131c` 系の暗い藍）で塗られている。
- 中央に朱色（`#b83b2e`）の立方体が1つ描かれている（黒一色＝WebGL失敗 ではない）。
- 床にグリッド（格子）が見える。
- 画面外へのはみ出し・webスクロールバーが無い（契約 §2 UI思想）。

- [ ] コミットする（スクショ画像は .gitignore 済なので含めない）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add src/engine/smoke.ts test-e2e/smoke.spec.ts tools/screenshot.mjs && git commit -m "feat: add Three.js smoke scene + playwright screenshot harness (screen renders)"
```

---

### Task G1.7: G1 完了ゲート（全スクリプトの疎通確認＋README 最小）

**Files**
- Create: `/Users/takayamanoboruhaku/projects/onmyoji-rpg/README.md`
- Test: 全 npm スクリプト（`typecheck` / `test:run` / `build` / `screenshot`）の疎通

**Interfaces**
- Consumes: G1.1〜G1.6 の全成果物
- Produces: G2 以降が乗れる「ビルド/型/テスト/スクショが全て緑」の基盤と、開発手順の最小 README。

G1 の締め。新規コードはほぼ無く、検証は「全スクリプトが成功する」の機械確認。

- [ ] 型チェックが通る（コマンドと期待出力 PASS）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npm run typecheck && echo "TYPECHECK_OK"
```
期待出力: `error TS` 無しで最終行に `TYPECHECK_OK`。

- [ ] Vitest（純粋ロジック）が通る（コマンドと期待出力 PASS）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npm run test:run 2>&1 | tail -6
```
期待出力: `Test Files  1 passed (1)` / `Tests  9 passed (9)`（現状 `config.test.ts` のみ。G5 で combat テストが増える）。

- [ ] ビルドが通る（コマンドと期待出力 PASS）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && npm run build 2>&1 | tail -4
```
期待出力: `✓ built in` を含みエラー行無し。

- [ ] `README.md` を作成する（完全な内容・開発手順の最小）

`/Users/takayamanoboruhaku/projects/onmyoji-rpg/README.md`:
```markdown
# onmyoji-rpg ─ 京音都・陰陽師アクションRPG（Phase0 技術プロト）

Vite + TypeScript + Three.js (r0.171.0 固定) の単一テストシーン技術プロト。
正典は `docs/bible/phase0-shared-contract.md`。型・命名・パスはそれに逐語一致させる。

## 必要環境
- Node 23 系（`.nvmrc` 参照）
- npm 10 系

## セットアップ
```bash
npm install
npx playwright install chromium
```

## スクリプト
| コマンド | 内容 |
|---|---|
| `npm run dev` | dev サーバ起動（http://127.0.0.1:5173） |
| `npm run build` | 型チェック＋本番ビルド（`dist/`） |
| `npm run preview` | ビルド成果物のプレビュー |
| `npm run typecheck` | TypeScript 型チェック（strict） |
| `npm run test` | Vitest watch（純粋ロジック層のみ） |
| `npm run test:run` | Vitest 単発実行 |
| `npm run coverage` | Vitest カバレッジ（純粋層 80%+ 目標） |
| `npm run screenshot` | headless Chromium で `tools/shots/scene.png` を撮影 |

## アーキテクチャ
- 純粋ロジック層 `src/combat/*`・`src/combat-runtime/hitbox.ts`：Three.js を import せず Vitest で TDD。
- 描画/手触り層 `src/engine/*`・`src/entities/*`・`src/world/*`・`src/ui/*`：Three.js 依存。Playwright スクショ＋手動チェックリストで検証。
- 一方向データフロー（入力 → ロジック更新 → 描画）。

## 配信
GitHub Pages 想定。Vite `base: './'`（相対パス）でサブパス配信に対応。
```

- [ ] git 作業ツリーがクリーンであることを確認する（コマンドと期待出力）

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git add README.md && git status --short
```
期待出力: `A  README.md` のみ（`dist/` `node_modules/` `tools/shots/` は .gitignore 済で出ない。他に未追跡/変更が出たら追跡漏れなので調査）。

- [ ] コミットする

```bash
cd /Users/takayamanoboruhaku/projects/onmyoji-rpg && git commit -m "docs: add README (setup, scripts, architecture) — G1 foundation complete"
```

---

**G1 完了の定義（Definition of Done）**: `npm run typecheck` / `npm run test:run` / `npm run build` が全て緑。`npm run screenshot` で藍墨背景に朱色の箱とグリッドが描かれた `tools/shots/scene.png` が出る。`package-lock.json` に `three@0.171.x` が固定コミット済み。`dist/index.html` のアセット参照が相対パス。後続 G2（`engine/smoke.ts` を `renderer.ts`/`camera.ts`/`loop.ts` に置換）が着手可能。
